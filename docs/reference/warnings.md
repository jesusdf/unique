# Diagnostic catalog (`UNIQUE-NNNN` warnings & errors)

> **Generated — do not edit by hand.** Produced by `python scripts/generate_reference_docs.py` from the `UNIQUE-NNNN` registry (`src/unique/core/diagnostics.py`) and the rationale side-table (`src/unique/core/rationales.py`). The CI freshness gate (`python scripts/generate_reference_docs.py --check`) fails the build if this file drifts from the source data.

One row per stable diagnostic code the transpiler can emit. `code` is the grep/suppress token (`-- UNIQUE-1234: …`); `message` is the human-readable template rendered live at the emission site. The `construct` / `reason` / `example` / `divergence` columns are the B31 rationale — populated for 182 of 233 codes; the rest render `_(rationale pending)_` until a rationale is added (the coverage ratchet in `tests/unit/core/test_diagnostics.py` drives that count down). Anchor each row as `warnings.md#unique-1234`.

| Code | Category | Message template | Construct | Reason | Example | Divergence |
|---|---|---|---|---|---|---|
| <a id="unique-1001"></a>`UNIQUE-1001` | statement | MERGE rewritten as INSERT ... ON DUPLICATE KEY UPDATE; requires a UNIQUE or PRIMARY KEY on<br>({on_cols} | MERGE with one unconditional MATCHED UPDATE and one unconditional NOT MATCHED INSERT (→ MySQL) | MySQL has no MERGE statement at all; the canonical single-UPDATE/single-INSERT shape is the only one<br>with a faithful MySQL rewrite (INSERT ... SELECT ... ON DUPLICATE KEY UPDATE), and that rewrite<br>relies on a UNIQUE or PRIMARY KEY covering the ON columns to detect the 'matched' case the same way<br>the MERGE's ON did. | [`TestMergeToMySQL::test_upsert_signals_key_assumption`](../../tests/unit/core/test_merge_mysql.py) | Faithful when a UNIQUE/PRIMARY KEY covers the ON columns (the common case); the key assumption is<br>only noted, not verified against a live schema. |
| <a id="unique-1002"></a>`UNIQUE-1002` | statement | SET IDENTITY_INSERT {_ii_tbl} {_ii_st} is a T-SQL session directive with no cross-engine equivalent;<br>dropped (the target accepts an explicit value into an identity/serial/ auto_increment column)<br>(docs/03-unsupported.md | SET IDENTITY_INSERT ON/OFF (T-SQL) | T-SQL requires IDENTITY_INSERT ON before a script may supply its own value for an identity column;<br>no other engine has an explicit identity-override mode — they simply accept an explicit value in the<br>INSERT column list — so the ON/OFF bracket has nothing to map to. | [`reda-ts-identity-insert`](../../tests/fixtures/challenge/) | The INSERT's data is faithful; the two SET directives degrade to carriers (one warning). |
| <a id="unique-1003"></a>`UNIQUE-1003` | statement | statement preserved as a comment; the specific reason is carried at runtime | A source construct with no per-site conversion — the shared RawSQL fallback carrier (e.g. a SELECT-<br>list set-returning function relocated to FROM, which MySQL has no inline table-function form for) | This code is the converter's generic 'could not emit this node' escape valve — used at many<br>unrelated call sites for whatever sqlglot reason string reaches it (an unmodeled expression type, a<br>foreign built-in with no target form, a parse error) — so the concrete engine-level reason is<br>carried in the comment text at runtime rather than fixed per code. | [`TestGenerateSeriesFrom::test_srf_in_select_list_moved_to_from`](../../tests/integration/test_chall<br>enge.py) | Warned limit — the statement is preserved as a comment; the base rows are unaffected on targets<br>where the construct does map. |
| <a id="unique-1004"></a>`UNIQUE-1004` | statement | NULLS FIRST/LAST index ordering has no {dialect} equivalent; dropped (it affects only the index's<br>physical null order, not query results | NULLS FIRST/LAST index-column ordering (→ Oracle/T-SQL/MySQL) | NULLS FIRST/LAST in an index definition is Oracle-rejected outright (ORA-00907) and has no<br>T-SQL/MySQL spelling at all; it affects only the index's physical null placement, not query results,<br>so it is dropped rather than block the CREATE INDEX. | [`postgresql-drop2-NULLS`](../../tests/fixtures/challenge/) | Warned limit — physical null-order only, no result-set impact. |
| <a id="unique-1005"></a>`UNIQUE-1005` | statement | statement preserved as a comment; the specific reason is carried at runtime | An index over an expression, e.g. CREATE INDEX ... ((concat(a, b))) (→ T-SQL) | T-SQL has no expression/functional index at all — an index can only cover column names, not an<br>arbitrary expression — so there is no CREATE INDEX form to target; a computed column plus an index<br>on it is the closest workaround, but it is a schema change the transpiler cannot make unilaterally. | [`TestWave204ExpressionIndexes::test_expression_index_carrier_tsql`](../../tests/integration/test_pg<br>_source_wave1.py) | Warned limit — statement preserved as a comment; add a computed column and index it by hand. |
| <a id="unique-1006"></a>`UNIQUE-1006` | statement | {reason} | A partial (WHERE-filtered) UNIQUE index whose predicate is outside T-SQL's filtered-index grammar | T-SQL's filtered-index WHERE accepts only a narrow comparison grammar (error 10735 otherwise); a<br>broader UNIQUE index (one covering more rows than the filtered original) would reject rows the<br>source's partial index allowed, so widening it silently would change what INSERTs succeed — the<br>whole CREATE INDEX degrades rather than emit a subtly different constraint. | [`TestIndexRebuildRefinements::test_complex_predicate_unique_degrades`](../../tests/integration/test<br>_pg_source_wave1.py) | Warned limit — statement preserved as a comment. |
| <a id="unique-1007"></a>`UNIQUE-1007` | statement | partial-index predicate dropped (no {dialect} filtered-index form); the index is broader than the<br>source's: … | A partial (WHERE-filtered) non-unique index whose predicate is outside T-SQL's filtered-index<br>grammar | T-SQL's filtered-index WHERE accepts only a narrow comparison grammar (error 10735 otherwise);<br>unlike the UNIQUE case, a broader plain index changes no INSERT/UPDATE behavior — it only covers<br>more rows than strictly needed — so the predicate is dropped and the (now unfiltered) index is kept<br>rather than degrading the whole statement. | [`TestIndexRebuildRefinements::test_complex_predicate_nonunique_drops_where`](../../tests/integratio<br>n/test_pg_source_wave1.py) | Warned limit — the index is broader than the source's (covers more rows); query results are<br>unaffected. |
| <a id="unique-1008"></a>`UNIQUE-1008` | statement | PostgreSQL unique indexes treat NULLs as distinct; T-SQL allows a single NULL per unique index | A plain (unfiltered) UNIQUE index on a nullable column (→ T-SQL) | PostgreSQL (and Oracle/MySQL) treat every NULL in a unique index as distinct from every other NULL,<br>so multiple NULL rows are all allowed; T-SQL's UNIQUE index allows only a single NULL row — a<br>genuine behavioral divergence with no rewrite that preserves PostgreSQL's semantics on T-SQL. | [`TestPgIndexToTsql::test_nameless_index_gets_a_name`](../../tests/integration/test_pg_source_wave1.<br>py) | Warned limit — a second NULL row that PostgreSQL allowed raises a duplicate-key error on T-SQL. |
| <a id="unique-1009"></a>`UNIQUE-1009` | statement | T-SQL has no boolean value type; NOT of a non-predicate (e.g. NOT NULL) has no equivalent -- see<br>docs/03-unsupported.md | NOT applied to a non-predicate operand, e.g. (NOT NULL) IS NULL (→ T-SQL) | PostgreSQL/MySQL/Oracle evaluate NOT under three-valued logic — the negation of NULL is still NULL —<br>but T-SQL has no boolean value type at all: NOT requires a genuine predicate operand, so NOT of a<br>bare NULL/column literal is error 4145, not a value. | [`pg-not-null-is-null`](../../tests/fixtures/challenge/) | Warned limit on T-SQL — the operand degrades to a carrier; faithful (1/TRUE) on the other three<br>engines. |
| <a id="unique-1010"></a>`UNIQUE-1010` | statement | T-SQL ALTER COLUMN defaults the column to NULL; the script does not define {table}.{col}'s<br>nullability, so it cannot be re-stated — verify the column keeps its constraint | ALTER COLUMN ... TYPE on a column whose nullability the script never declares (→ T-SQL) | T-SQL's ALTER COLUMN <type> re-states the FULL column definition and defaults to NULL when<br>nullability is omitted, silently dropping an existing NOT NULL; without an in-script CREATE TABLE<br>(or --db-url) to harvest the column's declared nullability from, the transpiler cannot know whether<br>re-stating NULL is safe. | [`TestB10RunningColumnTypeAlterNullability::test_unknown_column_warns`](../../tests/integration/test<br>_pg_source_wave1.py) | Warned limit — the ALTER COLUMN still runs (best-effort NULL); verify the column keeps its original<br>constraint. |
| <a id="unique-1011"></a>`UNIQUE-1011` | statement | named DEFAULT constraint {n} dropped (defaults are anonymous on this engine | _(rationale pending)_ | — | — | — |
| <a id="unique-1012"></a>`UNIQUE-1012` | statement | {dialect} does not support INCLUDE covering columns; dropped: … | _(rationale pending)_ | — | — | — |
| <a id="unique-1013"></a>`UNIQUE-1013` | statement | {dialect} does not support filtered indexes; dropped predicate:… | _(rationale pending)_ | — | — | — |
| <a id="unique-1014"></a>`UNIQUE-1014` | statement | {clauses} -- tsql-only, no {dialect} equivalent (physical index clause | Physical index storage clause, e.g. WITH (FILLFACTOR = n) (T-SQL) | FILLFACTOR and sibling physical clauses reserve per-page free space — a storage-tuning knob with no<br>logical effect on query results; Oracle and MySQL CREATE INDEX have no equivalent clause. | [`reda-ts-index-fillfactor-mysql`](../../tests/fixtures/challenge/) | Faithful in result (storage-only); the clause is dropped and kept in a restorable note. |
| <a id="unique-1015"></a>`UNIQUE-1015` | statement | MySQL default collation is case-insensitive, so DISTINCT/ordering on a string column merges case-<br>differing values | DISTINCT / ORDER BY over a string column under MySQL's default collation | MySQL's default collation is case-insensitive, so DISTINCT / GROUP BY / ORDER BY treat 'a' and 'A'<br>as equal and collapse them into one row; the case-sensitive PostgreSQL/Oracle defaults keep them<br>distinct — a row-count divergence no ORDER BY LOWER() rewrite can bridge without column-level<br>collation visibility. | [`my-distinct-case`](../../tests/fixtures/challenge/) | Documented limit, warned — deduplicated row counts may differ. |
| <a id="unique-1016"></a>`UNIQUE-1016` | statement | MySQL has no GROUP BY …; the base grouping is kept and the super-aggregate (subtotal) rows are<br>omitted | GROUP BY CUBE / ROLLUP / GROUPING SETS super-aggregate rows (→ MySQL) | MySQL has no CUBE / GROUPING SETS and only a trailing WITH ROLLUP, so a multi-element grouping's<br>subtotal (super-aggregate) rows cannot be produced; only the base grouping is kept. | [`pg-grouping-fn`](../../tests/fixtures/challenge/) | Warned — base grouping kept; subtotal rows omitted. |
| <a id="unique-1017"></a>`UNIQUE-1017` | statement | MySQL has no multi-element GROUP BY (CUBE/ROLLUP/ GROUPING SETS combined); the base grouping is kept<br>and the super-aggregate (subtotal) rows are omitted | _(rationale pending)_ | — | — | — |
| <a id="unique-1018"></a>`UNIQUE-1018` | statement | T-SQL FOR XML/JSON row serialization has no cross-engine equivalent; the clause is dropped and the<br>base rows are returned instead (see docs/03-unsupported.md | _(rationale pending)_ | — | — | — |
| <a id="unique-1019"></a>`UNIQUE-1019` | statement | MySQL SQL_CALC_FOUND_ROWS has no equivalent here; the full row count for a following FOUND_ROWS() is<br>not computed — run a separate COUNT(*) query | SQL_CALC_FOUND_ROWS on a SELECT with LIMIT (MySQL) → other engines | SQL_CALC_FOUND_ROWS is a MySQL-only optimizer hint that makes a following FOUND_ROWS() return the<br>row count the LIMIT would otherwise have discarded; no other engine has an equivalent two-statement<br>result-caching mechanism. | [`mysql-qdrop-SQL_CALC_FOU`](../../tests/fixtures/challenge/) | Warned limit — the hint is dropped; run a separate COUNT(*) query for the discarded-row total. |
| <a id="unique-1020"></a>`UNIQUE-1020` | statement | all-defaults INSERT has no Oracle spelling without the column list; original preserved | INSERT INTO t VALUES () — an all-defaults insert with no column list (→ Oracle) | Oracle's INSERT grammar has no all-defaults form: a column-less VALUES () is rejected outright, and<br>there is no DEFAULT VALUES keyword (T-SQL) or empty-parens shorthand (MySQL/PostgreSQL) to fall back<br>on without knowing the table's column list. | [`TestEmptyValuesAndIsNullValue::test_empty_values_oracle_degrades`](../../tests/integration/test_pg<br>_source_wave1.py) | Warned limit — statement preserved as a comment; supply an explicit column list to insert on Oracle. |
| <a id="unique-1021"></a>`UNIQUE-1021` | statement | INSERT preserved as a comment; the specific reason is carried at runtime | An upsert (PG ON CONFLICT DO UPDATE-shaped) INSERT with no in-script conflict target to harvest | PostgreSQL's ON CONFLICT DO UPDATE needs an explicit conflict target (column list or constraint);<br>the source (e.g. a MySQL ON DUPLICATE KEY UPDATE, which names none) leaves it to be inferred from<br>the table's declared key, and without an in-script CREATE TABLE (or --db-url) there is nothing to<br>infer it from. | [`test_mysql_upsert_without_known_key_degrades_whole`](../../tests/unit/core/test_upsert.py) | Warned limit — statement preserved as a comment rather than a bare INSERT that would raise a<br>duplicate-key error. |
| <a id="unique-1022"></a>`UNIQUE-1022` | statement | conflict target assumed to be (…) from the table's key; the MySQL source names no explicit target<br>(fires on any unique key | MySQL INSERT ... ON DUPLICATE KEY UPDATE lowered to PostgreSQL ON CONFLICT (col) (or the MERGE ON<br>clause) | MySQL's ON DUPLICATE KEY UPDATE names no explicit conflict target — it fires on any unique or<br>primary key the row collides with — while PostgreSQL's ON CONFLICT and the MERGE ON clause both<br>require one; the transpiler infers it from the table's declared key (visible in-script or via --db-<br>url) and must flag that inference, since a different key could exist. | [`test_mysql_update_to_pg_needs_harvested_key`](../../tests/unit/core/test_upsert.py) | Warned assumption — faithful when the harvested key is the one that would actually collide. |
| <a id="unique-1023"></a>`UNIQUE-1023` | statement | INSERT IGNORE also swallows other errors (bad values, FK violations), not only duplicate keys —<br>unlike PG ON CONFLICT DO NOTHING | PostgreSQL ON CONFLICT DO NOTHING lowered to MySQL INSERT IGNORE | MySQL's INSERT IGNORE is a broader error-suppressor than PostgreSQL's DO NOTHING (or the MERGE<br>insert-only forms): it also swallows non-duplicate errors — bad values, foreign-key violations —<br>that the source would have raised, so the mapping is the closest available but not behavior-<br>identical on error. | [`test_pg_do_nothing_to_mysql_is_insert_ignore`](../../tests/unit/core/test_upsert.py) | Warned limit — faithful on a genuine duplicate key; a non-duplicate error that the source would<br>raise is instead swallowed. |
| <a id="unique-1024"></a>`UNIQUE-1024` | statement | MySQL ON DUPLICATE KEY UPDATE fires on ANY unique/primary key, not a single named conflict target | PostgreSQL ON CONFLICT (col) DO UPDATE lowered to MySQL ON DUPLICATE KEY UPDATE | MySQL's ON DUPLICATE KEY UPDATE fires on a collision with ANY unique or primary key on the table,<br>not only the single named conflict target the PostgreSQL source declared — a genuine any-key-vs-one-<br>key semantic gap with no MySQL syntax to narrow it. | [`test_pg_update_to_mysql_uses_on_duplicate_key`](../../tests/unit/core/test_upsert.py) | Warned limit — faithful when the named conflict target is the table's only unique key. |
| <a id="unique-1025"></a>`UNIQUE-1025` | statement | MERGE ON key assumed to be (…) from the table's key; the source names no explicit conflict target | MySQL INSERT ... ON DUPLICATE KEY UPDATE lowered to a T-SQL MERGE | MySQL's ON DUPLICATE KEY UPDATE names no explicit conflict column, but a MERGE's ON clause requires<br>one; the transpiler assumes the table's declared key (harvested in-script or via --db-url) is the<br>intended join column and must flag that assumption rather than silently pick a possibly-wrong key. | [`test_mysql_update_to_tsql_lowers_to_merge_with_harvested_key`](../../tests/unit/core/test_upsert.p<br>y) | Warned assumption — faithful when the harvested key is the one that would actually collide. |
| <a id="unique-1026"></a>`UNIQUE-1026` | statement | Oracle has no UPDATE ... FROM and this join shape (no ON condition) cannot become a correlated<br>subquery; rewrite as a MERGE. Original | _(rationale pending)_ | — | — | — |
| <a id="unique-1027"></a>`UNIQUE-1027` | statement | @@ROWCOUNT has no top-level {dialect} equivalent | A top-level (non-cursor-context) T-SQL @@ROWCOUNT reference (→ PostgreSQL/Oracle) | @@ROWCOUNT is context-free only inside T-SQL's own session state; outside a T-SQL procedural body<br>(the procedural path maps it to GET DIAGNOSTICS / SQL%ROWCOUNT using surrounding statement context)<br>there is nothing to map it to, so a bare top-level reference gets a documented neutral value<br>instead. | [`TestSystemGlobalsInDml::test_rowcount_pg_neutral`](../../tests/integration/test_pg_source_wave1.py<br>) | Warned limit — degrades to the constant 0 (MySQL maps to ROW_COUNT() instead and is faithful). |
| <a id="unique-1028"></a>`UNIQUE-1028` | statement | @@FETCH_STATUS has no top-level {dialect} equivalent; it is cursor state | A top-level (non-cursor-context) T-SQL @@FETCH_STATUS reference (→ PostgreSQL/Oracle/MySQL) | @@FETCH_STATUS is cursor state by nature — the procedural path maps it using the surrounding FETCH's<br>context (FOUND / handler flags / cursor%FOUND) — so a context-free top-level reference has nothing<br>to bind to and gets a documented neutral instead. | [`TestFetchStatusTopLevel::test_fetch_status_neutral_pg`](../../tests/integration/test_pg_source_wav<br>e1.py) | Warned limit — degrades to the constant 0 outside cursor context. |
| <a id="unique-1029"></a>`UNIQUE-1029` | statement | @@ERROR has no top-level {dialect} equivalent; use an exception handler | _(rationale pending)_ | — | — | — |
| <a id="unique-1030"></a>`UNIQUE-1030` | statement | @@VERSION -> {fn}; version string differs per engine | T-SQL @@VERSION (→ PostgreSQL/MySQL) | PostgreSQL's version() and MySQL's VERSION() return each engine's own version string in its own<br>format, never T-SQL's — the closest available function, but the returned value can never equal the<br>source's. | [`ts-spid-version`](../../tests/fixtures/challenge/) | Warned limit — mapped to the target's native version function; the string value differs from<br>T-SQL's. |
| <a id="unique-1031"></a>`UNIQUE-1031` | statement | @@VERSION has no Oracle equivalent outside v$version | T-SQL @@VERSION (→ Oracle) | Oracle's version string lives only in the v$version view, which needs a query (and often elevated<br>privileges) — not a scalar expression a statement can splice in — so there is no drop-in replacement<br>expression at all. | [`ts-spid-version`](../../tests/fixtures/challenge/) | Warned limit — degrades to NULL rather than a fabricated version string. |
| <a id="unique-1032"></a>`UNIQUE-1032` | statement | @@SPID -> {fn}; session id differs per engine | T-SQL @@SPID (→ PostgreSQL/MySQL/Oracle) | Every engine spells its session/connection identifier differently (PostgreSQL pg_backend_pid(),<br>MySQL CONNECTION_ID(), Oracle SYS_CONTEXT('USERENV','SID')) and the value is inherently per-<br>connection, so it can never equal T-SQL's @@SPID even when mapped to the engine's closest<br>equivalent. | [`ts-spid-version`](../../tests/fixtures/challenge/) | Warned limit — mapped to the target's own session-id function; the numeric value differs from<br>T-SQL's. |
| <a id="unique-1033"></a>`UNIQUE-1033` | statement | SQL%ROWCOUNT has no top-level {dialect} equivalent | _(rationale pending)_ | — | — | — |
| <a id="unique-1034"></a>`UNIQUE-1034` | statement | TABLESAMPLE ({what}) has no MySQL equivalent — all rows returned (docs/03-unsupported.md | TABLESAMPLE (→ MySQL) | MySQL has no row-sampling clause at all (and sampling is inherently non-deterministic besides), so<br>there is no way to return a subset of rows equivalent to the source's sample. | [`pg-tablesample`](../../tests/fixtures/challenge/) | Warned limit — all rows are returned instead of a sample. |
| <a id="unique-1035"></a>`UNIQUE-1035` | statement | TABLESAMPLE by row count has no Oracle SAMPLE form (docs/03-unsupported.md | _(rationale pending)_ | — | — | — |
| <a id="unique-1036"></a>`UNIQUE-1036` | statement | TABLESAMPLE by row count has no PostgreSQL equivalent (docs/03-unsupported.md | _(rationale pending)_ | — | — | — |
| <a id="unique-1037"></a>`UNIQUE-1037` | statement | source had WITH TIES; MySQL has no equivalent — rows tying the last one are not returned (see<br>docs/03-unsupported.md | TOP ... WITH TIES (T-SQL) / FETCH FIRST ... WITH TIES (→ MySQL) | MySQL's LIMIT has no WITH TIES equivalent — it caps at a fixed row count with no provision for<br>including rows that tie the last one on the ORDER BY key. | [`ts-top-with-ties`](../../tests/fixtures/challenge/) | Warned limit — rows tying the last returned row are not included (PostgreSQL/Oracle FETCH FIRST ...<br>WITH TIES are faithful). |
| <a id="unique-1038"></a>`UNIQUE-1038` | statement | source was TOP n PERCENT; {dialect} has no LIMIT PERCENT — emitted as a row count, adjust to<br>CEIL(n/100 * total_rows) if a true percentage is required | TOP n PERCENT (T-SQL) → PostgreSQL/MySQL | PostgreSQL's LIMIT and MySQL's LIMIT both take a row count, not a percentage of the result set's<br>size — and the result set's size is not knowable at transpile time (it depends on the query's own<br>WHERE/JOIN), so n cannot be converted to an equivalent row count without executing the query first. | [`TestParseSelectLimit::test_top_percent_postgresql_documented`](../../tests/unit/core/test_converte<br>r.py) | Warned limit — n is emitted as a literal row count, not n percent of the actual result size; adjust<br>with CEIL(n/100 * total_rows) if a true percentage is required. |
| <a id="unique-1039"></a>`UNIQUE-1039` | ddl | Oracle WITH LOCAL TIME ZONE and PostgreSQL timestamptz both display column {name} in the session<br>time zone (same instant, session-dependent wall clock) (docs/03-unsupported.md | Oracle TIMESTAMP WITH LOCAL TIME ZONE column (→ PostgreSQL) | PostgreSQL timestamptz has the identical session-time-zone display behaviour as Oracle's LTZ (live-<br>verified: the same instant shows 12:00 in a UTC session and 07:00 in a New York session), so the<br>column maps directly rather than losing anything. | [`ora-dttypes`](../../tests/fixtures/challenge/) | Faithful — same instant, same session-dependent wall-clock display. |
| <a id="unique-1040"></a>`UNIQUE-1040` | ddl | tsql has no session-local timestamp type — column {name} WITH LOCAL TIME ZONE maps to<br>DATETIMEOFFSET; the value's instant is kept but the session-time-zone display is not reproduced<br>(docs/03-unsupported.md | Oracle TIMESTAMP WITH LOCAL TIME ZONE column (→ T-SQL) | T-SQL has no session-local timestamp type; the column maps to DATETIMEOFFSET, which keeps a fixed<br>stored offset instead of re-deriving the session's own time zone on every read. | [`ora-dttypes`](../../tests/fixtures/challenge/) | Warned limit — the value's instant is kept, but the session-time-zone display is not reproduced. |
| <a id="unique-1041"></a>`UNIQUE-1041` | ddl | mysql has no session-local timestamp type — column {name} WITH LOCAL TIME ZONE maps to TIMESTAMP;<br>the value's instant is kept but the session-time-zone display is not reproduced<br>(docs/03-unsupported.md | Oracle TIMESTAMP WITH LOCAL TIME ZONE column (→ MySQL) | MySQL has no session-local timestamp type; the column maps to TIMESTAMP, which normalizes to UTC<br>storage instead of re-deriving the session's own time zone on every read. | [`ora-dttypes`](../../tests/fixtures/challenge/) | Warned limit — the value's instant is kept, but the session-time-zone display is not reproduced. |
| <a id="unique-1042"></a>`UNIQUE-1042` | ddl | Oracle has no TIME type — column … stores the time of day as INTERVAL DAY TO SECOND{tz}<br>(docs/03-unsupported.md | A TIME/TIMETZ column (→ Oracle) | Oracle has no bare TIME (or TIME WITH TIME ZONE) column type, so the time-of-day value is stored as<br>INTERVAL DAY TO SECOND (a duration since midnight) instead. | [`pg-dttypes`](../../tests/fixtures/challenge/) | Faithful for TIME (the same time-of-day value, stored as a duration); warned limit for TIMETZ — the<br>zone offset is dropped. |
| <a id="unique-1043"></a>`UNIQUE-1043` | ddl | PostgreSQL INTERVAL mixes year-month and day-second fields; column … is mapped to INTERVAL DAY TO<br>SECOND — year-month values need a separate INTERVAL YEAR TO MONTH column (docs/03-unsupported.md | A bare PostgreSQL INTERVAL column, with no YEAR TO MONTH/DAY TO SECOND qualifier (→ Oracle) | PostgreSQL's INTERVAL mixes year-month and day-second fields in one value; Oracle splits INTERVAL<br>into two distinct column types, so the column maps to INTERVAL DAY TO SECOND and any year-month<br>component has nowhere to go. | [`pg-dttypes`](../../tests/fixtures/challenge/) | Warned limit — year-month values need a separate INTERVAL YEAR TO MONTH column on Oracle. |
| <a id="unique-1044"></a>`UNIQUE-1044` | ddl | {dialect} has no INTERVAL column type — column … keeps the interval as text (docs/03-unsupported.md | An INTERVAL column (→ T-SQL / MySQL) | T-SQL has no interval type at all, and MySQL's INTERVAL is only an arithmetic qualifier (e.g.<br>INTERVAL 1 DAY), never a column type, so the interval value is kept as text instead. | [`ora-tz-interval`](../../tests/fixtures/challenge/) | Warned limit — the value is kept as text, not usable in interval arithmetic on the target. |
| <a id="unique-1045"></a>`UNIQUE-1045` | ddl | MySQL fractional-seconds precision caps at 6 — column … precision … clamped to 6<br>(docs/03-unsupported.md | A fractional-seconds column precision above 6 (DATETIME(n)/TIMESTAMP(n)/TIME(n) with n>6) (→ MySQL) | MySQL's sub-second precision caps at microseconds (6 digits); a higher source precision (e.g. T-SQL<br>DATETIME2(7)) has no wider MySQL fractional-seconds type to map onto. | [`ts-datetimeoffset`](../../tests/fixtures/challenge/) | Warned limit — precision clamped to 6; sub-microsecond digits lost. |
| <a id="unique-1046"></a>`UNIQUE-1046` | ddl | {dialect} has no bit-string type — column … BIT(…) stores its numeric value as {mapped}<br>(docs/03-unsupported.md | MySQL multi-bit BIT(n>1) column (→ Oracle / T-SQL) | Neither engine has a bit-string column type; a multi-bit BIT is a 64-bit numeric value, not a<br>boolean, so it maps to a wide NUMBER/NUMERIC that holds the same value instead. | [`my-bintypes`](../../tests/fixtures/challenge/) | Warned limit — the numeric value is preserved but the bit-string type semantics (bitwise operations,<br>display) are not. |
| <a id="unique-1047"></a>`UNIQUE-1047` | ddl | MySQL SET type on {col_name} has no {dialect} equivalent; stored as {varchar}({total_len}). Allowed<br>members: {quoted_values} | _(rationale pending)_ | — | — | — |
| <a id="unique-1048"></a>`UNIQUE-1048` | ddl | LIKE clone copies column structure only here; the source's indexes/keys are not cloned | T-SQL structure-clone CREATE TABLE t2 LIKE t1 (→ T-SQL / Oracle) | T-SQL/Oracle have no native CREATE TABLE ... LIKE; the faithful idiom is an empty SELECT INTO / CTAS<br>(WHERE 1 = 0), which clones column structure only — indexes, keys and constraints are not part of<br>that idiom. | [`TestCreateTableLikeClone::test_like_tsql_empty_select_into`](../../tests/integration/test_pg_sourc<br>e_wave1.py) | Warned limit — the source's indexes/keys are not cloned. |
| <a id="unique-1049"></a>`UNIQUE-1049` | ddl | source IDENTITY (START {seed} INCREMENT {step}) has no MySQL column form — AUTO_INCREMENT starts at<br>1, steps by 1 (docs/03-unsupported.md | IDENTITY / GENERATED AS IDENTITY with a non-default START/INCREMENT (→ MySQL) | MySQL's only identity form is AUTO_INCREMENT, whose seed is a table option (AUTO_INCREMENT = n) with<br>no per-column START/INCREMENT — a non-default seed/step cannot be reproduced as a column clause. | [`ora-identity-opts`](../../tests/fixtures/challenge/) | Warned limit — AUTO_INCREMENT starts at 1 and steps by 1. |
| <a id="unique-1050"></a>`UNIQUE-1050` | ddl | MySQL ON UPDATE column clause has no equivalent on the target engine | MySQL column-level ON UPDATE CURRENT_TIMESTAMP auto-refresh clause (→ other engines) | No other engine has a column-level auto-refresh-on-update clause; the behaviour can only be<br>reproduced there with an explicit AFTER UPDATE trigger. | [`mysql-drop2-ON`](../../tests/fixtures/challenge/) | Warned limit — the column keeps its DEFAULT but is no longer auto-refreshed; add a trigger to<br>restore the behaviour. |
| <a id="unique-1051"></a>`UNIQUE-1051` | ddl | column {col_name} collation/charset (…) has no portable {dialect} equivalent; the column uses the<br>default collation (comparisons/ordering may differ) — set it explicitly on the target or supply the<br>source DB connection | A column-level COLLATE clause (→ a different target engine) | Collation names are engine-specific catalog identifiers with no cross-engine mapping; without a live<br>database connection, Unique cannot resolve what collation the source column actually uses. | [`postgresql-drop4-COLLATE`](../../tests/fixtures/challenge/) | Warned limit — the column uses the target's default collation; comparisons/ordering may differ. |
| <a id="unique-1052"></a>`UNIQUE-1052` | ddl | column {col_name} was INVISIBLE (excluded from SELECT *) on the source; {dialect} has no invisible-<br>column attribute, so the column is now visible to SELECT * (docs/03-unsupported.md | MySQL/Oracle INVISIBLE column attribute (→ PostgreSQL / T-SQL) | PostgreSQL and T-SQL have no invisible-column attribute (a column excluded from SELECT *), so it<br>cannot be reproduced there. | [`red2-my-invisible-column-drop`](../../tests/fixtures/challenge/) | Warned limit — the column becomes visible to SELECT *, changing that query's result shape. |
| <a id="unique-1053"></a>`UNIQUE-1053` | ddl | PostgreSQL UNIQUE … NULLS NOT DISTINCT (NULLs compare equal) has no {dialect} equivalent; a plain<br>UNIQUE treats NULLs as distinct (docs/03-unsupported.md | PostgreSQL UNIQUE ... NULLS NOT DISTINCT (→ other engines) | Only PostgreSQL 15+ has a NULLS NOT DISTINCT unique-constraint modifier (NULLs compare equal, so<br>only one NULL row is allowed); every other engine's UNIQUE always treats NULLs as distinct. | [`pg-unique-nulls-notdistinct`](../../tests/fixtures/challenge/) | Warned limit — degrades to a plain UNIQUE; multiple NULL rows become allowed on the target. |
| <a id="unique-1054"></a>`UNIQUE-1054` | ddl | T-SQL forbids a cascading action on a self-referencing FK (error 1785); downgraded to NO ACTION —<br>emulate with an AFTER trigger if the automatic action is required (docs/03-unsupported.md | Cascading referential action on a self-referencing FK (→ T-SQL) | T-SQL rejects a cascading action on a self-referencing foreign key outright (error 1785 at CREATE<br>TABLE time) — an engine restriction, not a missing feature. | [`my-self-fk`](../../tests/fixtures/challenge/) | Warned limit — downgraded to ON DELETE NO ACTION; emulate the cascade with an AFTER trigger. |
| <a id="unique-1055"></a>`UNIQUE-1055` | ddl | Oracle has no ON DELETE SET DEFAULT referential action; dropped (FK reverts to NO ACTION) — emulate<br>with an AFTER DELETE trigger if required (docs/03-unsupported.md | Foreign-key ON DELETE SET DEFAULT referential action (→ Oracle) | Oracle foreign keys support only CASCADE/SET NULL/NO ACTION — SET DEFAULT raises ORA-03001<br>('unimplemented feature') if shipped verbatim. | [`red2-pg-fk-ondelete-setdefault-oracle`](../../tests/fixtures/challenge/) | Warned limit — the action is dropped (FK reverts to NO ACTION); emulate with an AFTER DELETE trigger<br>if required. |
| <a id="unique-1056"></a>`UNIQUE-1056` | ddl | T-SQL In-Memory OLTP storage option(s) [{opts}] have no {dialect} equivalent; the table is created<br>as a regular disk-based table (no logical/value difference | T-SQL In-Memory OLTP table storage option(s) (MEMORY_OPTIMIZED / DURABILITY) (→ other engines) | No other engine has an in-memory table storage mode; the option is a physical-storage clause with no<br>logical effect on query results. | [`tsql-drop5-MEMORY_OPTIM`](../../tests/fixtures/challenge/) | Faithful (storage-only) — the table becomes a regular disk-based table with the same logical<br>content. |
| <a id="unique-1057"></a>`UNIQUE-1057` | ddl | MySQL table default collation/charset (…) has no portable {dialect} equivalent; string columns use<br>the default collation (comparisons/ordering may differ) — set it explicitly on the target or supply<br>the source DB connection | MySQL table-level default COLLATE/CHARSET clause (→ other engines) | Same underlying gap as UNIQUE-1051 but table-scoped: collation names are engine-specific and<br>unresolvable without a live database connection. | [`mysql-drop5-utf8mb4`](../../tests/fixtures/challenge/) | Warned limit — string columns use the target's default collation. |
| <a id="unique-1058"></a>`UNIQUE-1058` | ddl | view modifier {mod} is not portable on {dialect}; dropped | A non-portable CREATE VIEW modifier (e.g. SCHEMABINDING, ALGORITHM=, DEFINER=, SQL SECURITY) with no<br>native form on the target | These modifiers are single-engine syntax with no equivalent option elsewhere (MATERIALIZED is<br>handled separately, natively, on Oracle/PostgreSQL, so it never reaches this drop). | [`red2-pg-matview-oracle-falsewarn`](../../tests/fixtures/challenge/) | Warned limit — the modifier is dropped; the view's query and columns are otherwise faithful. |
| <a id="unique-1059"></a>`UNIQUE-1059` | ddl | MySQL has no sequences (use an AUTO_INCREMENT column); original preserved | DROP SEQUENCE (→ MySQL) | MySQL has no sequence object at all (identity is expressed only via AUTO_INCREMENT columns), so<br>there is nothing for a DROP SEQUENCE to target. | [`TestDropSequenceMySql::test_mysql_degrades_to_documented_carrier`](../../tests/integration/test_or<br>acle_mysql_tail.py) | Warned limit — degrades to a documented carrier; the original statement is preserved as a comment. |
| <a id="unique-1060"></a>`UNIQUE-1060` | ddl | MySQL has no user-defined types; original preserved | DROP TYPE (→ MySQL) | MySQL has no user-defined type in any form (no CREATE TYPE/DOMAIN equivalent), so a DROP TYPE has<br>nothing to target. | [`TestMysqlUserTypesDegrade::test_drop_type_degrades_mysql`](../../tests/integration/test_pg_source_<br>wave1.py) | Warned limit — degrades to a documented carrier; the original statement is preserved. |
| <a id="unique-1061"></a>`UNIQUE-1061` | ddl | {dialect} DROP INDEX requires the owning table, which the source statement does not carry; original<br>preserved | A DROP INDEX <name> with no owning table (→ T-SQL / MySQL) | T-SQL and MySQL both require the owning table in DROP INDEX (index names are table-scoped there);<br>Oracle/PostgreSQL index names are schema-scoped, so a source statement from either carries no table<br>for the target to require. | [`test_drop_index_without_table_never_ships_invalid`](../../tests/integration/test_ddl_rename_dropin<br>dex.py) | Warned limit — degrades to a documented carrier rather than shipping a syntactically incomplete DROP<br>INDEX. |
| <a id="unique-1062"></a>`UNIQUE-1062` | ddl | PostgreSQL DROP TRIGGER requires the owning table (ON tbl), which the source statement does not<br>carry; original preserved | A schema-scoped DROP TRIGGER <name> with no owning table (→ PostgreSQL) | PostgreSQL trigger names are per-table (DROP TRIGGER requires ON <table>); T-SQL/MySQL/Oracle<br>trigger names are schema-scoped, so a source statement from any of them carries no table to supply. | [`TestDropTriggerOnTable::test_sourceless_on_degrades_to_pg`](../../tests/integration/test_pg_source<br>_wave1.py) | Warned limit — degrades to a documented carrier; the original statement is preserved. |
| <a id="unique-1063"></a>`UNIQUE-1063` | expression | … (…) — see docs/03-unsupported.md | A construct with no cross-engine mapping, kept as a documented carrier (e.g. T-SQL AT TIME ZONE, or<br>the T-SQL CLR spatial '::' method-call operator) | The generic degrade path for a value Unique recognizes but cannot compute on another engine — used<br>e.g. for AT TIME ZONE (Oracle/MySQL have no such operator; PostgreSQL/T-SQL's own session-tz-<br>dependent display differs) and T-SQL's geometry/geography '::' static-method call (no other engine<br>has CLR types). | [`ts-at-time-zone`](../../tests/fixtures/challenge/) | Warned limit — degrades to NULL + annotation; valid only on the source engine. |
| <a id="unique-1064"></a>`UNIQUE-1064` | expression | Oracle SESSIONTIMEZONE is session- dependent; the mapped expression reports this session's<br>zone/offset in the target's own format (docs/03-unsupported.md | Oracle bare SESSIONTIMEZONE global (→ other engines) | SESSIONTIMEZONE reports the connecting session's own UTC offset — a per-session value with no fixed<br>cross-engine equivalent; the mapped expression reports the TARGET session's own zone in its native<br>format instead. | [`ora-tz-funcs`](../../tests/fixtures/challenge/) | Warned limit — the value is session-dependent on the target too and may not match the source<br>session's zone. |
| <a id="unique-1065"></a>`UNIQUE-1065` | expression | Oracle has no {_what} type — value kept as text (docs/03-unsupported.md | CAST(... AS TIME) and other Oracle-absent value types | Oracle has no bare TIME (or plain INTERVAL) type, so the value is kept as text with a documented<br>carrier rather than an invalid cast. | [`my-cast-time`](../../tests/fixtures/challenge/) | Warned limit — value kept as text. |
| <a id="unique-1066"></a>`UNIQUE-1066` | expression | MySQL JSON type has no faithful cross-engine equivalent (T-SQL has no JSON type; canonical JSON<br>spacing differs on PG/Oracle) — value kept as text — see docs/03-unsupported.md | MySQL JSON type / CAST(... AS JSON) | MySQL's JSON type has no faithful cross-engine equivalent — T-SQL has no JSON type at all, and<br>canonical JSON spacing differs on PostgreSQL/Oracle — so the value is kept as text. | [`my-cast-json`](../../tests/fixtures/challenge/) | Warned limit — value kept as text. |
| <a id="unique-1067"></a>`UNIQUE-1067` | expression | PostgreSQL geometric type … has no cross-engine equivalent — value kept as text<br>(docs/03-unsupported.md | PostgreSQL geometric type (point/line/…) cast or column | PostgreSQL's geometric types have no cross-engine model (MySQL's spatial POINT is a different WKB<br>type), so the value is kept as text. | [`pg-cast-point`](../../tests/fixtures/challenge/) | Warned limit — value kept as text. |
| <a id="unique-1068"></a>`UNIQUE-1068` | expression | PostgreSQL NaN/Infinity has no {dialect} numeric equivalent (docs/03-unsupported.md | A PostgreSQL numeric NaN/Infinity literal cast to a numeric type (→ MySQL / T-SQL / Oracle) | Only PostgreSQL's numeric type has a NaN/Infinity value; MySQL/T-SQL/Oracle DECIMAL/NUMBER silently<br>collapse a 'NaN' cast to 0, so the special value has no faithful representation there. | [`pg-nan-cmp`](../../tests/fixtures/challenge/) | Warned limit — the carrier documents that the value cannot be reproduced numerically on the target. |
| <a id="unique-1069"></a>`UNIQUE-1069` | expression | MySQL UNSIGNED has no {dialect} equivalent; unsigned wraparound not preserved<br>(docs/03-unsupported.md | MySQL UNSIGNED integer type / CAST(... AS UNSIGNED) | No other engine has an UNSIGNED integer type; the value is mapped to a signed NUMERIC/NUMBER, so<br>unsigned wraparound semantics are not preserved. | [`my-cast-convert`](../../tests/fixtures/challenge/) | Warned limit — unsigned wraparound not preserved. |
| <a id="unique-1070"></a>`UNIQUE-1070` | expression | Oracle DEFAULT ... ON CONVERSION ERROR has no {dialect} error-safe cast for this type; fallback<br>dropped -- see docs/03-unsupported.md | _(rationale pending)_ | — | — | — |
| <a id="unique-1071"></a>`UNIQUE-1071` | expression | T-SQL FOR XML/JSON row serialization has no cross-engine equivalent — see docs/03-unsupported.md | A scalar subquery serialized via T-SQL FOR XML/JSON (→ other engines) | FOR XML/JSON row-serialization inside a (SELECT ... FOR XML/JSON) scalar subquery has no cross-<br>engine equivalent; dropping only the clause would ship the multi-column rows raw into a scalar<br>context (ORA-00913 'too many values'), so the whole scalar degrades instead. | [`ts-for-xml`](../../tests/fixtures/challenge/) | Warned limit — degrades to NULL + annotation. |
| <a id="unique-1072"></a>`UNIQUE-1072` | expression | …; no {dialect} mapping — review | _(rationale pending)_ | — | — | — |
| <a id="unique-1073"></a>`UNIQUE-1073` | expression | MySQL date arithmetic on a non-datetime string literal yields NULL (docs/03-unsupported.md | MySQL date arithmetic on a non-datetime string literal (e.g. DATE_ADD('not-a-date', INTERVAL ...)) | MySQL's own date-arithmetic functions yield NULL when the first argument doesn't parse as a<br>date/time value; folding the literal at transpile time reproduces that NULL rather than emitting an<br>invalid cast on another engine. | [`my-timestr-plus`](../../tests/fixtures/challenge/) | Warned limit — the value folds to NULL to match MySQL's own behaviour. |
| <a id="unique-1074"></a>`UNIQUE-1074` | expression | MySQL DATE - DATE is a numeric YYYYMMDD subtraction; normalized to a day count<br>(docs/03-unsupported.md | MySQL DATE - DATE subtraction | MySQL's DATE - DATE operator is a numeric YYYYMMDD subtraction (e.g. 2020-03-01 - 2020-01-01 = 200),<br>not a day count; the meaningful day-count value (60, matching every other engine's date subtraction)<br>is emitted instead. | [`my-date-diff-minus`](../../tests/fixtures/challenge/) | Warned limit — normalized to a day count rather than MySQL's own YYYYMMDD arithmetic result. |
| <a id="unique-1075"></a>`UNIQUE-1075` | expression | timestamp difference is an INTERVAL with no {dialect} equivalent; emitted as a SECOND count<br>(docs/03-unsupported.md | timestamp - timestamp subtraction (→ T-SQL / MySQL) | PostgreSQL/Oracle timestamp subtraction yields an INTERVAL; T-SQL and MySQL have no interval value<br>type, so the difference is computed as a SECOND count via DATEDIFF/TIMESTAMPDIFF instead. | [`TestTimestampDifferenceDegrade::test_tsql_and_mysql_degrade_with_warning`](../../tests/integration<br>/test_challenge.py) | Warned limit — a SECOND count replaces the source's INTERVAL value (same information, different<br>type). |
| <a id="unique-1076"></a>`UNIQUE-1076` | expression | windowed string aggregation (string-agg OVER …) has no {dialect} equivalent — see<br>docs/03-unsupported.md | LISTAGG(...) WITHIN GROUP (...) OVER (...) — windowed string aggregation (Oracle) | T-SQL STRING_AGG and MySQL GROUP_CONCAT can never carry an OVER clause, and PostgreSQL rejects an<br>ORDER-BY aggregate used as a window function — there is no running-string-aggregate form to target. | [`ora-listagg-over`](../../tests/fixtures/challenge/) | Warned limit — degrades to a NULL value plus annotation. |
| <a id="unique-1077"></a>`UNIQUE-1077` | expression | a GROUPS window frame has no {dialect} equivalent (only ROWS/RANGE, and no faithful rewrite with<br>ORDER-BY ties) — see docs/03-unsupported.md | GROUPS window frame (PostgreSQL / Oracle) | T-SQL and MySQL implement only ROWS and RANGE frame units; a GROUPS frame spans whole peer groups,<br>and no ROWS/RANGE combination reproduces that boundary when the ORDER BY key has ties. | [`pg-window-groups-frame`](../../tests/fixtures/challenge/) | Warned limit on T-SQL/MySQL — degrades to a NULL carrier; faithful on Oracle/PostgreSQL. |
| <a id="unique-1078"></a>`UNIQUE-1078` | expression | a window frame … has no {dialect} equivalent (T-SQL/MySQL have no EXCLUDE, and no faithful<br>ROWS/RANGE rewrite) — see docs/03-unsupported.md | A window frame EXCLUDE clause (CURRENT ROW / GROUP / TIES) (→ T-SQL / MySQL) | T-SQL and MySQL implement no EXCLUDE option on a window frame at all, and there is no faithful<br>ROWS/RANGE rewrite that reproduces excluding specific peer rows from the frame. | [`red2-pg-window-exclude-current`](../../tests/fixtures/challenge/) | Warned limit on T-SQL/MySQL — degrades to a NULL carrier; faithful on PostgreSQL/Oracle, which<br>support EXCLUDE natively. |
| <a id="unique-1079"></a>`UNIQUE-1079` | expression | {fn} unit '{unit_sql}' has no {dialect} equivalent — the value was not computed<br>(docs/03-unsupported.md | _(rationale pending)_ | — | — | — |
| <a id="unique-1080"></a>`UNIQUE-1080` | expression | T-SQL has no sequence CURRVAL; capture NEXT VALUE FOR {seq} in a variable — see<br>docs/03-unsupported.md | Sequence CURRVAL — current value without advancing (→ T-SQL) | T-SQL has NEXT VALUE FOR but no CURRVAL; there is no way to read a sequence's current value without<br>advancing it. | [`ora-seq-use`](../../tests/fixtures/challenge/) | Warned limit — capture NEXT VALUE FOR in a variable instead. |
| <a id="unique-1081"></a>`UNIQUE-1081` | expression | DATEPART(WEEKDAY) is @@DATEFIRST-dependent; assumes the session default (Sunday=1 | _(rationale pending)_ | — | — | — |
| <a id="unique-1082"></a>`UNIQUE-1082` | expression | Oracle stores an empty string as NULL (docs/03-unsupported.md) | An empty-string result on Oracle ('' ≡ NULL) | Oracle has no on-disk representation for an empty string distinct from NULL — an empty-string result<br>becomes NULL — so a value that is '' on other engines cannot be reproduced there. | [`pg-repeat-negative`](../../tests/fixtures/challenge/) | Warned limit — the empty string surfaces as Oracle NULL. |
| <a id="unique-1083"></a>`UNIQUE-1083` | expression | DATEPART(WEEKDAY) is @@DATEFIRST-dependent; converted assuming the session default (Sunday=1 | DATEPART(WEEKDAY, d) (T-SQL) | DATEPART(WEEKDAY) depends on the session @@DATEFIRST setting, which Unique cannot observe at<br>transpile time; the conversion assumes the T-SQL default (Sunday = 1). | [`reda-ts-datepart-weekday`](../../tests/fixtures/challenge/) | Warned — correct under the default @@DATEFIRST = 7; a session that changed DATEFIRST will see a<br>different result. |
| <a id="unique-1084"></a>`UNIQUE-1084` | expression | Oracle ROUND(date, '{fmt}') (nearest {fmt} boundary) has no faithful {dialect} equivalent — the<br>value was not computed (docs/03-unsupported.md | Oracle ROUND(date, fmt) — a date rounded to the nearest fmt boundary (→ other engines) | No other engine has a nearest-boundary date-rounding function; only Oracle's own ROUND(date, fmt)<br>computes this, so the general case (any fmt) has no cross-engine formula. | [`red2-ora-round-date-fmt`](../../tests/fixtures/challenge/) | Warned limit — degrades to a NULL carrier; not computed off Oracle. |
| <a id="unique-1085"></a>`UNIQUE-1085` | expression | Oracle TRUNC(date, '{raw_up}') has no {dialect} equivalent — the value was not computed<br>(docs/03-unsupported.md | Oracle TRUNC(date, fmt) with a format model that has no portable truncation (e.g. 'W' week-of-month) | Most Oracle TRUNC format models map to a portable truncation unit (day, month, year, ISO week, ...),<br>but a few (like 'W', week-of-the-month) have no equivalent boundary on any other engine. | [`red2-ora-trunc-format-unmapped`](../../tests/fixtures/challenge/) | Warned limit — degrades to a NULL carrier off Oracle; native TRUNC is kept on Oracle. |
| <a id="unique-1086"></a>`UNIQUE-1086` | expression | EXTRACT({part}) has no {dialect} equivalent — the value was not computed (docs/03-unsupported.md | _(rationale pending)_ | — | — | — |
| <a id="unique-1087"></a>`UNIQUE-1087` | expression | Oracle INSTR with an occurrence count or backward (negative-start) search has no portable equivalent<br>for non-literal arguments — see docs/03-unsupported.md | Oracle INSTR with a non-literal occurrence count or a non-literal backward (negative-start) search | INSTR's occurrence/backward-search semantics fold to the engine-agnostic computed value only when<br>every argument is a literal (at transpile time); a non-literal (column/expression) argument cannot<br>be folded, and no other engine's positional-search function reproduces Oracle's occurrence/backward<br>semantics directly. | [`TestLiteralFolds::test_oracle_instr_nonliteral_degrades`](../../tests/integration/test_challenge.p<br>y) | Warned limit — degrades to a NULL carrier for the non-literal case; literal arguments still fold to<br>the correct value. |
| <a id="unique-1088"></a>`UNIQUE-1088` | expression | MySQL UpdateXML has no cross-engine equivalent (PG lacks it; T-SQL .modify() and Oracle UPDATEXML<br>differ) — see docs/03-unsupported.md | MySQL UpdateXML() (→ other engines) | UpdateXML has no cross-engine equivalent — PostgreSQL lacks it, and T-SQL .modify() / Oracle<br>UPDATEXML differ in shape and semantics. | [`my-xml-fns`](../../tests/fixtures/challenge/) | Warned limit. |
| <a id="unique-1089"></a>`UNIQUE-1089` | expression | collation names are engine-specific and cannot match across engines (docs/03-unsupported.md | COLLATION(x) — the collation name of a value (→ other engines) | Collation names are engine-specific catalog identifiers (e.g. MySQL's utf8mb4_0900_ai_ci vs Oracle's<br>NLS-based names) with no cross-engine mapping, even though the function itself exists on multiple<br>engines. | [`my-collation-fn`](../../tests/fixtures/challenge/) | Warned limit — the source's engine-specific collation name is preserved but will not match the<br>target's naming. |
| <a id="unique-1090"></a>`UNIQUE-1090` | expression | Oracle REGEXP_SUBSTR capture-group extraction (6th arg) has no MySQL equivalent<br>(docs/03-unsupported.md | Oracle REGEXP_SUBSTR capture-group extraction (6th arg) (→ MySQL) | MySQL's REGEXP_SUBSTR has no capture-group argument, so the sub-group extraction cannot be<br>expressed; the portable (str, pat, pos, occ) subset is emitted. | [`ora-regexp-group`](../../tests/fixtures/challenge/) | Warned limit — capture-group extraction not reproduced. |
| <a id="unique-1091"></a>`UNIQUE-1091` | expression | MySQL has no TRANSLATE and a nested-REPLACE emulation is order-dependent (not equivalent) — see<br>docs/03-unsupported.md | Oracle TRANSLATE(s, from, to) (→ MySQL) | TRANSLATE is native on PostgreSQL/T-SQL, but MySQL has no TRANSLATE function; a nested-REPLACE<br>emulation is order-dependent (each REPLACE can rematch a previous substitution's output) and is not<br>equivalent to TRANSLATE's simultaneous single-pass character mapping. | [`ora-translate3`](../../tests/fixtures/challenge/) | Warned limit — degrades to a NULL carrier on MySQL; native and faithful on PostgreSQL/T-SQL. |
| <a id="unique-1092"></a>`UNIQUE-1092` | expression | SUBSTRING(x FROM POSIX pattern) has no T-SQL regex equivalent — see docs/03-unsupported.md | SUBSTRING(x FROM POSIX-regex-pattern) (→ T-SQL) | T-SQL has no POSIX regular-expression engine, so a POSIX-pattern SUBSTRING (native on Oracle<br>REGEXP_SUBSTR / MySQL) has no equivalent there. | [`pg-substring-regex`](../../tests/fixtures/challenge/) | Warned limit — degrades to a NULL carrier on T-SQL; faithful on Oracle/MySQL via REGEXP_SUBSTR. |
| <a id="unique-1093"></a>`UNIQUE-1093` | expression | SUBSTRING(x FROM SIMILAR-TO pattern FOR escape) has no cross-engine equivalent (SQL-regex metachars<br>differ from POSIX) — see docs/03-unsupported.md | SUBSTRING(x FROM SIMILAR-TO-pattern FOR escape) — the SQL-standard regex form | The SQL-standard SIMILAR TO pattern syntax uses different metacharacters (%, _, #"..."# capture<br>markers) than POSIX regex, so no faithful cross-engine rewrite exists on engines whose regex<br>functions expect POSIX syntax. | [`pg-substring-escape`](../../tests/fixtures/challenge/) | Warned limit — degrades to a NULL carrier. |
| <a id="unique-1094"></a>`UNIQUE-1094` | expression | Oracle stores an empty string as NULL (docs/03-unsupported.md | An empty-string function result on Oracle (e.g. SUBSTR yielding '') | The same underlying limit as Oracle's NULL-equals-empty-string storage (UNIQUE-1082/1207), applied<br>to a computed (not stored) empty-string result — Oracle collapses it to NULL at the point of<br>computation too. | [`my-fsubstr`](../../tests/fixtures/challenge/) | Warned limit — the empty string surfaces as Oracle NULL. |
| <a id="unique-1095"></a>`UNIQUE-1095` | expression | MySQL VALUES(col) outside INSERT … ON DUPLICATE KEY UPDATE is NULL | MySQL VALUES(col) used outside an INSERT ... ON DUPLICATE KEY UPDATE statement | MySQL's VALUES(col) function only has meaning inside the ON DUPLICATE KEY UPDATE clause of the very<br>INSERT it appears in (reading the row that would have been inserted); used elsewhere (e.g. inside a<br>stored procedure's ordinary UPDATE) it is NULL on MySQL itself, so the transpiler reproduces that<br>NULL. | [`TestWave223ValuesFnOutfile::test_values_fn_null_oracle`](../../tests/integration/test_pg_source_wa<br>ve1.py) | Faithful — matches MySQL's own out-of-context NULL behaviour. |
| <a id="unique-1096"></a>`UNIQUE-1096` | expression | EXTRACT(EPOCH FROM interval) has no portable equivalent (T-SQL/MySQL have no interval value type) —<br>see docs/03-unsupported.md | EXTRACT(EPOCH FROM interval) (PostgreSQL) | T-SQL and MySQL have no interval value type, so the epoch (total seconds) of an interval value has<br>no portable equivalent. | [`pg-epoch`](../../tests/fixtures/challenge/) | Warned limit — degrades to NULL + annotation (EPOCH FROM a timestamp is still computed). |
| <a id="unique-1097"></a>`UNIQUE-1097` | expression | EXTRACT(MICROSECONDS FROM TIME) has no Oracle equivalent (no TIME type) — see docs/03-unsupported.md | EXTRACT(MICROSECONDS FROM TIME) (→ Oracle) | Oracle has no TIME type, so the microseconds field of a TIME value has no Oracle equivalent. | [`pg-frac-seconds`](../../tests/fixtures/challenge/) | Warned limit. |
| <a id="unique-1098"></a>`UNIQUE-1098` | expression | PG format() with %L/width/positional specifiers has no cross-engine equivalent — see<br>docs/03-unsupported.md | PostgreSQL format() with a %I/%L specifier, a width, or a positional argument (→ other engines) | Only the plain %s-only template has a portable rewrite (string concatenation); %I (quoted<br>identifier) and %L (quoted literal) specifiers, width modifiers and positional (%1$s) arguments have<br>no equivalent formatting primitive on other engines. | [`TestFormatFunc::test_complex_spec_degrades`](../../tests/integration/test_challenge.py) | Warned limit — degrades to a NULL carrier; a %s-only template still rewrites faithfully. |
| <a id="unique-1099"></a>`UNIQUE-1099` | expression | PG sha256/sha512 returns a bytea digest; other engines return a hex string (same digest, different<br>representation) — see docs/03-unsupported.md | PostgreSQL sha256()/sha512() (→ other engines) | PostgreSQL's sha256/sha512 return a bytea digest, while every other engine's equivalent hash<br>function returns a hex-encoded string — the underlying digest is identical, but the representation<br>differs and cannot be reconciled without an explicit encode() the source SQL doesn't have. | [`pg-hash-fns`](../../tests/fixtures/challenge/) | Warned limit — degrades to a NULL carrier; md5() and other hash functions with matching<br>representations still map faithfully. |
| <a id="unique-1100"></a>`UNIQUE-1100` | expression | MySQL CHAR({_n}) is a multi-byte byte string, not a single code point (docs/03-unsupported.md | MySQL CHAR(n) as a numeric-to-byte-string function | MySQL's CHAR(n) returns a multi-byte byte string (CHAR(256) = the 2-byte string 0x0100), not a<br>single Unicode code point like CHR/NCHAR, so the two cannot be equated. | [`my-char-256`](../../tests/fixtures/challenge/) | Warned limit — carrier flags the byte-string vs code-point difference. |
| <a id="unique-1101"></a>`UNIQUE-1101` | statement | {_cte_reason} | A data-modifying CTE, e.g. WITH ins AS (INSERT ... RETURNING ...) SELECT ... (PostgreSQL) → T-SQL | T-SQL's WITH only introduces read-only query CTEs — an INSERT/UPDATE/DELETE/MERGE inside a WITH body<br>is invalid there — so a data-modifying CTE has no T-SQL spelling at all. | [`TestZeroPushW7Batch::test_data_modifying_cte_carrier_tsql`](../../tests/unit/core/test_ir_first_fa<br>milies.py) | Warned limit — statement preserved as a comment. |
| <a id="unique-1102"></a>`UNIQUE-1102` | statement | MySQL NOT ENFORCED (a CHECK defined but not validated) has no target equivalent; enforced here | _(rationale pending)_ | — | — | — |
| <a id="unique-1103"></a>`UNIQUE-1103` | statement | FOR SHARE (shared row lock) has no Oracle equivalent (Oracle SELECT locking is FOR UPDATE,<br>exclusive); the shared lock is dropped (docs/03-unsupported.md | SELECT ... FOR SHARE — a shared row lock (→ Oracle) | Oracle SELECT locking is FOR UPDATE (exclusive) only — it has no shared-row-lock mode — so the<br>shared lock cannot be reproduced. | [`my-for-share`](../../tests/fixtures/challenge/) | Warned limit — the shared lock is dropped. |
| <a id="unique-1104"></a>`UNIQUE-1104` | statement | Oracle FOR UPDATE WAIT <n> (bounded lock wait) has no {dialect} equivalent; it blocks with the<br>default behavior (docs/03-unsupported.md | Oracle FOR UPDATE WAIT <n> — a bounded lock wait | PostgreSQL/MySQL offer only FOR UPDATE / NOWAIT / SKIP LOCKED, with no bounded-wait timeout, so the<br>WAIT <n> bound has no equivalent. | [`ora-forupdate-wait`](../../tests/fixtures/challenge/) | Warned limit — it blocks with the target's default behavior. |
| <a id="unique-1105"></a>`UNIQUE-1105` | statement | Oracle FOR UPDATE OF <column> selects which table's rows to lock; {dialect} FOR UPDATE OF takes<br>table names, so the OF list is dropped (every row read is locked) (docs/03-unsupported.md | Oracle FOR UPDATE OF <column> (→ PostgreSQL/MySQL) | Oracle's FOR UPDATE OF names a COLUMN, selecting which joined table's rows to lock via the column's<br>owning table; PostgreSQL and MySQL's FOR UPDATE OF instead takes a TABLE/alias name directly — an<br>incompatible argument shape, not just a renaming. | [`reda-ora-forupdate-of-col`](../../tests/fixtures/challenge/) | Warned limit — the source's column name is dropped rather than leaked unchanged into a table-name<br>position. |
| <a id="unique-1106"></a>`UNIQUE-1106` | statement | T-SQL has no expression/function index; add a computed column and index it (docs/03-unsupported.md | _(rationale pending)_ | — | — | — |
| <a id="unique-1107"></a>`UNIQUE-1107` | statement | T-SQL IDENTITY() in SELECT INTO reproduced as ROW_NUMBER (id values match); the identity/auto-<br>increment column property is not portable in a CREATE TABLE AS SELECT (docs/03-unsupported.md | _(rationale pending)_ | — | — | — |
| <a id="unique-1108"></a>`UNIQUE-1108` | statement | {dialect} has no ALTER … NOT VALID; the constraint is validated immediately (PostgreSQL defers it | ADD CONSTRAINT ... NOT VALID (PostgreSQL, deferred validation) → T-SQL/Oracle/MySQL | Only PostgreSQL can add a constraint without validating existing rows against it immediately; T-SQL,<br>Oracle and MySQL all validate an added constraint at ADD time, with no deferred-validation mode to<br>opt into. | [`pg-alter-notvalid`](../../tests/fixtures/challenge/) | Warned limit — the constraint definition is identical; the target validates existing rows<br>immediately instead of deferring. |
| <a id="unique-1109"></a>`UNIQUE-1109` | statement | TRUNCATE … CASCADE (also truncates FK-dependent tables) has no {dialect} equivalent; only this table<br>is truncated — truncate any dependents explicitly | TRUNCATE ... CASCADE (PostgreSQL, also truncates FK-dependent tables) → MySQL/T-SQL | Only Oracle's TRUNCATE has a CASCADE option matching PostgreSQL's; MySQL and T-SQL TRUNCATE truncate<br>only the named table, with no mechanism to also truncate its FK-dependent tables in one statement. | [`pg-truncate-restart`](../../tests/fixtures/challenge/) | Warned limit — only the named table is truncated; truncate dependent tables explicitly. |
| <a id="unique-1110"></a>`UNIQUE-1110` | statement | {dialect} has no ALTER COLUMN … USING conversion expression; convert the data manually. Statement<br>preserved as a comment | ALTER COLUMN ... TYPE t USING <non-trivial expression> (PostgreSQL) → T-SQL | T-SQL's ALTER COLUMN has no USING conversion-expression clause at all — it can only re-cast to the<br>new type using the engine's own implicit conversion, with no way to run an arbitrary expression over<br>the existing data during the ALTER. | [`TestWave199CteDeleteUsingAlterUsing::test_alter_using_expression_carriers`](../../tests/integratio<br>n/test_pg_source_wave1.py) | Warned limit — statement preserved as a comment; convert the data manually. |
| <a id="unique-1111"></a>`UNIQUE-1111` | statement | {dialect} needs the column's declared type to alter its nullability and the script does not define<br>{_nn_tbl_raw}.{_nn_col}; original postgresql statement preserved | _(rationale pending)_ | — | — | — |
| <a id="unique-1112"></a>`UNIQUE-1112` | statement | MySQL's only identity form is AUTO_INCREMENT (must be a key; a UNIQUE index is added | ADD COLUMN ... GENERATED ALWAYS AS IDENTITY / SERIAL (→ MySQL) | MySQL's only identity form is AUTO_INCREMENT, which MySQL additionally requires to be a key (error<br>1075 otherwise) — a constraint PostgreSQL/Oracle identity columns do not share — so a UNIQUE index<br>must be synthesized alongside the column. | [`pg-add-identity`](../../tests/fixtures/challenge/) | Faithful, with an added UNIQUE index noted as a MySQL-specific requirement, not part of the source<br>schema. |
| <a id="unique-1113"></a>`UNIQUE-1113` | statement | PostgreSQL GIN/GiST/BRIN index has no {dialect} equivalent (access-method specific); index omitted —<br>queries run unindexed (docs/03-unsupported.md | A GIN/GiST/BRIN index (PostgreSQL) → T-SQL/MySQL/Oracle | GIN/GiST/BRIN are PostgreSQL-specific access methods (inverted, generalized-search-tree, block-<br>range) with no equivalent index type on the other three engines — the choice of access method is<br>inherently engine-specific, unlike a plain B-tree index. | [`pg-gin-jsonb`](../../tests/fixtures/challenge/) | Warned limit — the index is omitted; queries that relied on it run unindexed. |
| <a id="unique-1114"></a>`UNIQUE-1114` | statement | expression index over a LOB-typed column is invalid on {dialect} (ORA-02327 / MySQL functional-index<br>restriction); index omitted — queries run unindexed (docs/03-unsupported.md | An expression index over a column that maps to a LOB type on the target | A source TEXT/CLOB-mapped column used inside an index expression is invalid on the target once the<br>type maps to a LOB (Oracle ORA-02327 forbids a LOB in a function-based index; MySQL's functional-<br>index grammar has the same restriction). | [`pg-expr-index`](../../tests/fixtures/challenge/) | Warned limit — the index is omitted; queries that relied on it run unindexed. |
| <a id="unique-1115"></a>`UNIQUE-1115` | statement | CONCURRENTLY (PostgreSQL's non-locking index build) has no {dialect} equivalent; the index is<br>created with the target's default locking | CREATE INDEX CONCURRENTLY (PostgreSQL's non-locking index build) → T-SQL/MySQL | CONCURRENTLY is a PostgreSQL build-strategy option (avoids locking the table during the build);<br>T-SQL and MySQL have no matching keyword — the resulting index is identical, only the build-time<br>locking behavior differs. | [`postgresql-drop2-CONCURRENTLY`](../../tests/fixtures/challenge/) | Faithful in result; the index is built with the target's default (locking) behavior instead. |
| <a id="unique-1116"></a>`UNIQUE-1116` | statement | MySQL session setting has no {dialect} equivalent; configure the session natively. | SET @@var = ... — a MySQL system-variable assignment via the @@ form (→ PostgreSQL/T-SQL/Oracle) | MySQL's @@ system variables (session or global) are engine-local tuning knobs (sql_mode, server_id,<br>...) with no meaning on any other engine — every other engine either lacks the setting or spells<br>session configuration a completely different way. | [`TestMysqlSessionKnobsDegrade::test_knob_degrades`](../../tests/integration/test_pg_source_wave1.py<br>) | Warned limit — statement preserved as a comment; configure the session natively on the target. |
| <a id="unique-1117"></a>`UNIQUE-1117` | statement | MySQL admin command has no {dialect} equivalent; run the target's own maintenance. | _(rationale pending)_ | — | — | — |
| <a id="unique-1118"></a>`UNIQUE-1118` | statement | {dialect} has no TEMPORARY sequences; statement preserved as a comment | CREATE TEMPORARY SEQUENCE (PostgreSQL) → T-SQL/MySQL/Oracle | A session-scoped (TEMPORARY) sequence exists only on PostgreSQL; T-SQL and Oracle sequences are<br>always permanent schema objects, and MySQL has no sequences at all, so there is no target form that<br>preserves the session-scoping. | [`TestZeroPushZ4bBatch::test_temporary_sequence_carriers`](../../tests/unit/core/test_ir_first_famil<br>ies.py) | Warned limit — statement preserved as a comment. |
| <a id="unique-1119"></a>`UNIQUE-1119` | statement | MySQL has no sequences; use an AUTO_INCREMENT column instead. Original | CREATE SEQUENCE (→ MySQL) | MySQL has no sequence object at all; an AUTO_INCREMENT column is the closest idiom, but it is a<br>column property, not a free-standing, shareable object the way a sequence is, so CREATE SEQUENCE has<br>no direct MySQL statement to become. | [`red2-pg-nextval-false-unmap`](../../tests/fixtures/challenge/) | Warned limit — statement preserved as a comment; use an AUTO_INCREMENT column instead. |
| <a id="unique-1120"></a>`UNIQUE-1120` | statement | SET SESSION AUTHORIZATION has no {dialect} equivalent; switch users natively. | _(rationale pending)_ | — | — | — |
| <a id="unique-1121"></a>`UNIQUE-1121` | statement | PostgreSQL session setting has no {dialect} equivalent; configure the session natively. | _(rationale pending)_ | — | — | — |
| <a id="unique-1122"></a>`UNIQUE-1122` | statement | {dialect} has no USE statement; connect to the target database/schema instead. | USE <database> (T-SQL/MySQL) → PostgreSQL/Oracle | USE switches the active database within a single connection on T-SQL and MySQL; PostgreSQL has no<br>SQL-level equivalent (only the psql client meta-command \c, which reconnects) and Oracle has none at<br>all (a schema is a database user, selected at connect time). | [`TestCrossDialectDDL::test_use_statement_documented_where_unsupported`](../../tests/integration/tes<br>t_cross_dialect.py) | Warned limit — statement preserved as a comment; connect to the target database/schema instead. |
| <a id="unique-1123"></a>`UNIQUE-1123` | statement | PostgreSQL column STORAGE tuning has no {dialect} equivalent; statement preserved as a comment | ALTER COLUMN ... SET STORAGE {PLAIN\|EXTERNAL\|EXTENDED\|MAIN} (PostgreSQL) → T-SQL/MySQL/Oracle | SET STORAGE tunes PostgreSQL's internal TOAST compression/out-of-line storage strategy for a column<br>— an engine-internal physical-storage knob with no logical effect on query results and no equivalent<br>concept on any other engine. | [`TestWave132Batch::test_set_storage_degrades_off_pg`](../../tests/integration/test_pg_source_wave1.<br>py) | Faithful in result (storage-only); statement preserved as a comment off PostgreSQL. |
| <a id="unique-1124"></a>`UNIQUE-1124` | statement | PostgreSQL's recursive-CTE SEARCH/CYCLE clause has no {dialect} equivalent; statement preserved as a<br>comment | A recursive CTE's SEARCH DEPTH/BREADTH FIRST BY ... SET ... / CYCLE clause (PostgreSQL 14+) →<br>T-SQL/MySQL/Oracle | SEARCH/CYCLE are PostgreSQL-only recursive-CTE ordering/cycle-detection clauses (SQL:1999 features<br>PostgreSQL adopted in 14); no other supported engine's recursive CTE grammar has them, and sqlglot<br>itself cannot parse them outside PostgreSQL. | [`TestWave191PgSearchCte::test_search_clause_carrier_mysql`](../../tests/integration/test_pg_source_<br>wave1.py) | Faithful on PostgreSQL; statement preserved as a comment elsewhere. |
| <a id="unique-1125"></a>`UNIQUE-1125` | statement | MySQL has no MERGE; rewrite as INSERT ... ON DUPLICATE KEY UPDATE. Original | A MERGE outside the canonical single-UPDATE/single-INSERT shape (→ MySQL) | MySQL has no MERGE statement; only the canonical two-branch shape has a faithful INSERT ... ON<br>DUPLICATE KEY UPDATE rewrite (UNIQUE-1001) — any other clause combination (multiple conditional<br>branches, a DELETE branch, NOT MATCHED BY SOURCE) has no MySQL equivalent to fall back to. | [`TestDDLPassthrough::test_merge_to_mysql_documented`](../../tests/integration/test_cross_dialect.py<br>) | Warned limit — statement preserved as a comment. |
| <a id="unique-1126"></a>`UNIQUE-1126` | statement | CTE with unsupported embedded DML preserved as a comment; reason carried at runtime | _(rationale pending)_ | — | — | — |
| <a id="unique-1127"></a>`UNIQUE-1127` | statement | BEGIN TRANSACTION dropped -- Oracle starts a transaction implicitly | BEGIN TRANSACTION (T-SQL) → Oracle | Oracle has no explicit 'start transaction' statement — a transaction begins implicitly with the<br>first DML — so an explicit BEGIN TRANSACTION has nothing to map to and is simply redundant on<br>Oracle, not an error to reproduce. | [`TestTsqlBeginTransaction::test_oracle_drops_with_warning`](../../tests/integration/test_challenge.<br>py) | Faithful — Oracle opens the same transaction implicitly on the first DML statement that follows. |
| <a id="unique-1128"></a>`UNIQUE-1128` | statement | T-SQL transactions have no READ … access mode; started as a regular transaction<br>(docs/03-unsupported.md | _(rationale pending)_ | — | — | — |
| <a id="unique-1129"></a>`UNIQUE-1129` | statement | READ COMMITTED is Oracle's default isolation level (no-op; kept as a note so a following SET<br>TRANSACTION mode statement can still open the transaction | _(rationale pending)_ | — | — | — |
| <a id="unique-1130"></a>`UNIQUE-1130` | statement | READ COMMITTED is Oracle's default isolation level (no-op; kept as a note so a following SET<br>TRANSACTION mode statement can still open the transaction | _(rationale pending)_ | — | — | — |
| <a id="unique-1131"></a>`UNIQUE-1131` | statement | Oracle has no {level} isolation level (supports READ COMMITTED/SERIALIZABLE only); statement<br>dropped. Original | _(rationale pending)_ | — | — | — |
| <a id="unique-1132"></a>`UNIQUE-1132` | statement | T-SQL SET TRANSACTION has no READ {mode} access mode; access mode dropped (docs/03-unsupported.md | SET TRANSACTION ISOLATION LEVEL <lvl> READ ONLY\|READ WRITE — combined isolation + access mode<br>(PostgreSQL) → T-SQL | T-SQL's SET TRANSACTION has no READ ONLY/READ WRITE access-mode clause at all — only ISOLATION LEVEL<br>is expressible — so the access-mode half of a combined PostgreSQL statement has no T-SQL spelling. | [`TestPgSetTransactionAccessMode::test_combined_tsql_keeps_isolation_drops_access_mode_with_warning`<br>](../../tests/integration/test_pg_source_wave1.py) | Warned limit — the isolation level is kept faithfully; the access mode is dropped. |
| <a id="unique-1133"></a>`UNIQUE-1133` | statement | T-SQL SET TRANSACTION has no READ {mode} access mode (docs/03-unsupported.md); statement dropped.<br>Original | A bare SET TRANSACTION READ ONLY\|READ WRITE with no isolation level (PostgreSQL) → T-SQL | T-SQL's SET TRANSACTION expresses only ISOLATION LEVEL, never an access mode; with no isolation<br>level present in the source either, there is nothing left in the statement that has a T-SQL spelling<br>at all. | [`TestPgSetTransactionAccessMode::test_bare_access_mode_tsql_degrades_with_warning`](../../tests/int<br>egration/test_pg_source_wave1.py) | Warned limit — the whole statement is dropped (nothing to keep); original preserved as a comment. |
| <a id="unique-1134"></a>`UNIQUE-1134` | statement | Oracle CONNECT BY / START WITH hierarchical query has no automatic equivalent; rewrite as a WITH<br>RECURSIVE CTE. Original | Oracle CONNECT BY / START WITH hierarchical query (→ PostgreSQL/MySQL/T-SQL), outside the shapes the<br>automatic recursive-CTE rewrite covers | CONNECT BY has no native equivalent on the other three engines; a WITH RECURSIVE CTE is the standard<br>rewrite, but it must be constructed by hand for shapes the automatic CONNECT-BY-to-CTE conversion<br>does not model, since a mechanical rewrite risks an incorrect recursion base or join. | [`TestNoSilentLoss::test_connect_by_to_postgresql_signals_warning`](../../tests/unit/core/test_no_si<br>lent_loss.py) | Warned limit — statement preserved as a comment; rewrite as a WITH RECURSIVE CTE by hand. |
| <a id="unique-1135"></a>`UNIQUE-1135` | statement | session-variable SELECT INTO has no cross-dialect equivalent; rewrite as the target's assignment<br>form. Original | _(rationale pending)_ | — | — | — |
| <a id="unique-1136"></a>`UNIQUE-1136` | statement | INSERT combines RETURNING and ON CONFLICT; rewrite as MERGE/upsert with result capture on {dialect}.<br>Original | An INSERT combining RETURNING and ON CONFLICT DO UPDATE in one statement (PostgreSQL) → MySQL | MySQL has neither RETURNING nor a named-target upsert clause; with both present at once there is no<br>partial mapping to fall back to (RETURNING alone would map to a follow-up SELECT, ON CONFLICT alone<br>to INSERT ... ON DUPLICATE KEY UPDATE, but the combination needs a MERGE-like statement plus result<br>capture that MySQL cannot express). | [`TestOnConflictMysqlAndEStrings::test_returning_on_conflict_mysql_degrades`](../../tests/integratio<br>n/test_pg_source_wave1.py) | Warned limit — statement preserved as a comment; rewrite as an upsert with a separate result-<br>capturing SELECT. |
| <a id="unique-1137"></a>`UNIQUE-1137` | statement | T-SQL OUTPUT … INTO <table> redirect has no PostgreSQL equivalent in a plain INSERT (it needs a<br>data-modifying CTE); the INTO target is dropped and the RETURNING result is kept<br>(docs/03-unsupported.md | T-SQL OUTPUT ... INTO <table> redirect (→ PostgreSQL) | PostgreSQL's RETURNING only returns a result set to the caller; it has no INTO <table> redirect<br>form, so the redirect cannot be expressed in a plain INSERT. | [`reda-ts-output-into`](../../tests/fixtures/challenge/) | Warned limit — the INTO redirect is dropped; the base DML and plain RETURNING are faithful. |
| <a id="unique-1138"></a>`UNIQUE-1138` | statement | Oracle has no UPDATE … FROM (rewrite with a correlated subquery or MERGE) and no top-level<br>RETURNING. Statement preserved as a comment | UPDATE ... FROM ... RETURNING (PostgreSQL) → Oracle | Oracle has neither UPDATE ... FROM (needs a correlated subquery or MERGE rewrite instead) nor a top-<br>level RETURNING (PL/SQL-only, ORA-63809 standalone) — with both present at once in the same<br>statement, there is no single Oracle statement shape left that can carry both the join and the<br>returned columns. | [`TestWave206OracleReturningShapes::test_returning_update_from_carrier_oracle`](../../tests/integrat<br>ion/test_pg_source_wave1.py) | Warned limit — statement preserved as a comment. |
| <a id="unique-1139"></a>`UNIQUE-1139` | statement | Oracle has no top-level RETURNING; the statement returned: {cols} | A top-level OUTPUT / RETURNING result set (→ Oracle) | Oracle's RETURNING is PL/SQL-only — it must target INTO bind variables and cannot stand alone in a<br>plain SQL statement (ORA-63809) — so a standalone OUTPUT/RETURNING has no Oracle equivalent. | [`reda-ts-output-into`](../../tests/fixtures/challenge/) | Warned limit — the DML runs; the returned result set is documented, not produced. |
| <a id="unique-1140"></a>`UNIQUE-1140` | statement | MySQL has no RETURNING/OUTPUT; the statement returned: {cols} | OUTPUT/RETURNING on an INSERT/UPDATE/DELETE (→ MySQL) | MySQL has no clause that returns affected-row values alongside a data-modifying statement —<br>LAST_INSERT_ID() only ever returns the last auto-increment value, not arbitrary column values, and<br>there is no set-returning DML form at all. | [`TestOutputClauseToMySQL::test_output_into_var_is_documented_not_returning`](../../tests/integratio<br>n/test_procedural.py) | Warned limit — the DML itself runs faithfully; the OUTPUT/RETURNING result set is documented, not<br>produced. |
| <a id="unique-1141"></a>`UNIQUE-1141` | statement | MERGE WHEN NOT MATCHED DO NOTHING has no faithful rewrite; reason carried at runtime | _(rationale pending)_ | — | — | — |
| <a id="unique-1142"></a>`UNIQUE-1142` | statement | MERGE clause has no faithful rewrite; reason carried at runtime | _(rationale pending)_ | — | — | — |
| <a id="unique-1143"></a>`UNIQUE-1143` | statement | T-SQL has no FOR UPDATE/FOR SHARE row-lock clause; lock the rows with a WITH (UPDLOCK, ROWLOCK)<br>table hint | A trailing FOR UPDATE / FOR SHARE row-lock clause (PostgreSQL/Oracle/MySQL) → T-SQL | T-SQL has no trailing row-lock clause on SELECT at all; row locking is instead requested via a WITH<br>(UPDLOCK, ROWLOCK) table hint on the FROM clause — a different syntactic position, not a drop-in<br>keyword substitution. | [`postgresql-qdrop-FOR`](../../tests/fixtures/challenge/) | Warned limit — the clause is dropped rather than left as invalid trailing syntax; add a WITH<br>(UPDLOCK, ROWLOCK) hint by hand for equivalent locking. |
| <a id="unique-1144"></a>`UNIQUE-1144` | statement | Unhandled … | _(rationale pending)_ | — | — | — |
| <a id="unique-1145"></a>`UNIQUE-1145` | statement | inline INDEX table element has no {dialect} equivalent form; index omitted — queries run unindexed.<br>Original: … | _(rationale pending)_ | — | — | — |
| <a id="unique-1146"></a>`UNIQUE-1146` | statement | PostgreSQL EXCLUDE constraint has no {dialect} equivalent; enforce the exclusion with a trigger.<br>Original: … | An EXCLUDE exclusion constraint (PostgreSQL) → T-SQL/MySQL/Oracle | EXCLUDE (e.g. preventing overlapping ranges via a GiST index) is a PostgreSQL-only constraint type<br>with no equivalent declarative constraint on any other engine; the same behavior there needs a hand-<br>written trigger. | [`postgresql-drop2-EXCLUDE`](../../tests/fixtures/challenge/) | Warned limit — the table itself stays valid; the exclusion is not enforced. Emulate it with a<br>trigger if required. |
| <a id="unique-1147"></a>`UNIQUE-1147` | statement | {dialect} requires an explicit type for the generated column {col_name}; original computed column: … | T-SQL computed column, e.g. total AS (a + b) PERSISTED (→ PostgreSQL/MySQL/Oracle) | T-SQL's computed-column syntax declares no explicit type at all (it is inferred from the<br>expression); PostgreSQL, MySQL and Oracle all require an explicit type on a generated column (live-<br>verified: MySQL rejects the typeless form too), and the transpiler cannot always infer one without<br>full expression type-checking. | [`TestCrossDialectDDL::test_computed_column_preserved`](../../tests/integration/test_cross_dialect.p<br>y) | Warned limit — the column definition is documented as a comment outside the (still valid) CREATE<br>TABLE column list. |
| <a id="unique-1148"></a>`UNIQUE-1148` | statement | FK ON UPDATE referential action dropped — Oracle has no ON UPDATE FK action (docs/03-unsupported.md | Foreign-key ON UPDATE referential action (→ Oracle) | Oracle foreign keys support only ON DELETE CASCADE / SET NULL — there is no ON UPDATE referential<br>action (ORA-00905). | [`reda-ts-fk-on-update`](../../tests/fixtures/challenge/) | Warned limit — ON UPDATE is dropped; reproduce it with a trigger if needed. |
| <a id="unique-1149"></a>`UNIQUE-1149` | expression | UNPIVOT has no {dialect} equivalent and the source columns are not visible to rewrite it as UNION<br>ALL — see docs/03-unsupported.md | _(rationale pending)_ | — | — | — |
| <a id="unique-1150"></a>`UNIQUE-1150` | expression | PIVOT has no {dialect} equivalent and the source columns are not visible to rewrite it as<br>conditional aggregation — see docs/03-unsupported.md | _(rationale pending)_ | — | — | — |
| <a id="unique-1151"></a>`UNIQUE-1151` | validation | output failed the {target} validity check ({reason}); original {source} batch preserved | A source-engine built-in with no form in the target's catalog (e.g. SOUNDEX → PostgreSQL) | A call that is a built-in of the source engine (clearly meant to run, not a user object) but absent<br>from the target's catalog would be rejected outright, so the whole statement degrades rather than<br>shipping an invalid call — the general unmapped-built-in gate. | [`ora-soundex`](../../tests/fixtures/challenge/) | Warned limit — the statement is preserved as a carrier and the failing built-in is named. |
| <a id="unique-1152"></a>`UNIQUE-1152` | procedural | type origin comment preserved from the source declaration | Oracle %TYPE / %ROWTYPE column-type reference (without --db-url) | Only Oracle supports %TYPE/%ROWTYPE; resolving the real column type needs a live catalog lookup<br>(ALL_TAB_COLUMNS) unavailable without a DB connection, so a permissive carrier type is emitted with<br>the original reference preserved for a faithful round-trip back to Oracle. | [`test_type_reference_documented_then_restored`](../../tests/integration/test_procedural.py) | Warned limit without --db-url (carrier type may not match exactly); faithful with --db-url or on an<br>Oracle→Oracle round-trip. |
| <a id="unique-1153"></a>`UNIQUE-1153` | procedural | PostgreSQL trigger function ('RETURNS TRIGGER') has no … equivalent (no trigger functions; the body<br>belongs to a CREATE TRIGGER). The non-portable translation is commented out below for review | PostgreSQL trigger function (CREATE FUNCTION ... RETURNS TRIGGER) (→ MySQL) | MySQL has no trigger functions — a trigger's body belongs directly to CREATE TRIGGER, not to a<br>separately-callable RETURNS TRIGGER function — so the PL/pgSQL function shape has nothing to bind<br>to. | [`TestPostgresTriggerToMySQL::test_trigger_function_degrades_with_warning`](../../tests/integration/<br>test_triggers.py) | Warned limit — the non-portable translation is preserved commented out for a manual rewrite. |
| <a id="unique-1154"></a>`UNIQUE-1154` | procedural | inline table-valued function ('RETURNS TABLE') has no direct equivalent. …. | T-SQL inline table-valued function (CREATE FUNCTION ... RETURNS TABLE AS RETURN (...)) | Neither MySQL nor PostgreSQL has an inline (single-statement, substituted-at-call-site) table-valued<br>function form; both would need a full multi-statement function/procedure rewritten by hand. | [`TestInlineTableValuedFunction::test_mysql_documents_and_comments_out`](../../tests/integration/tes<br>t_procedural.py) | Warned limit — documented and commented out rather than emitted as invalid RETURNS TABLE. |
| <a id="unique-1155"></a>`UNIQUE-1155` | procedural | trigger reads the T-SQL inserted/deleted pseudo-tables in a set-based way … cannot express; the<br>translation is preserved commented out for a manual rewrite | A trigger body that reads the T-SQL inserted/deleted pseudo-tables in a set-based way (FROM/JOIN<br>inserted\|deleted) the target cannot express | Once the set-based pseudo-table read degrades to a per-statement carrier (UNIQUE-1201), the<br>surrounding trigger no longer has a runnable body — shipping it partially would execute a half-empty<br>trigger per row, so the whole trigger is preserved commented out instead. | [`TestSetBasedTriggerRewrite::test_pure_set_based_to_mysql_documented`](../../tests/integration/test<br>_triggers.py) | Warned limit — the whole trigger becomes a documented carrier. |
| <a id="unique-1156"></a>`UNIQUE-1156` | procedural | Oracle COMPOUND TRIGGER … (… {events} ON …) has no automatic … equivalent — it collects affected<br>rows in a PL/SQL collection and re-aggregates in AFTER STATEMENT. Rewrite manually (PostgreSQL: a<br>statement-level trigger with REFERENCING NEW TABLE; MySQL: a row-level trigger that re-reads the<br>table). | Oracle COMPOUND TRIGGER (→ MySQL) | A compound trigger's BEFORE/AFTER EACH ROW + AFTER STATEMENT sections collect affected rows into a<br>PL/SQL collection and re-aggregate them statement-wide; MySQL has no equivalent mechanism (no<br>transition tables, no multi-timing trigger sections) to mechanically rewrite that accumulation into. | [`TestOracleCompoundTrigger::test_degrades_to_carrier_mysql`](../../tests/integration/test_triggers.<br>py) | Warned limit — documented for a manual rewrite (a MySQL row-level trigger that re-reads the table). |
| <a id="unique-1157"></a>`UNIQUE-1157` | procedural | PostgreSQL statement-level trigger delegating to a trigger function has no … equivalent (no<br>transition tables / trigger functions). Original binding | A PostgreSQL statement-level trigger delegating its body to a trigger function via EXECUTE FUNCTION<br>(→ MySQL) | MySQL has neither trigger functions nor transition tables, so a statement-level trigger that<br>references its bound function's transition-table reads has nothing to bind to. | [`TestPostgresTriggerToMySQL::test_trigger_binding_degrades_with_warning`](../../tests/integration/t<br>est_triggers.py) | Warned limit — documented carrier. |
| <a id="unique-1158"></a>`UNIQUE-1158` | procedural | {header} LOOP … has no … equivalent (no array type); statement preserved as a comment | A PostgreSQL FOREACH ... IN ARRAY LOOP (→ other engines) | The FOREACH-over-array loop is inherently array-typed, and no other engine has an array<br>column/variable type at all, so there is no array to iterate over on the target. | [`TestForeachArrayLoop::test_foreach_degrades_off_pg`](../../tests/integration/test_pg_source_wave1.<br>py) | Warned limit — statement preserved as a comment. |
| <a id="unique-1159"></a>`UNIQUE-1159` | procedural | PRAGMA … has no … equivalent; dropped. | An Oracle PL/SQL PRAGMA declaration (e.g. AUTONOMOUS_TRANSACTION) (→ other engines) | A PRAGMA is a compiler directive to the Oracle PL/SQL engine itself (no runtime SQL effect); no<br>other engine's procedural language has an equivalent compiler-directive mechanism. | [`TestPragmaAutonomousTransaction::test_off_oracle_pragma_never_ships_executable`](../../tests/integ<br>ration/test_oracle_mysql_tail.py) | Warned limit — dropped; the surrounding declarations and body still transpile. |
| <a id="unique-1160"></a>`UNIQUE-1160` | procedural | anonymous PL/SQL block has no top-level … equivalent; preserved below | A standalone (anonymous) PL/SQL block at the top level, with no enclosing CREATE PROCEDURE/FUNCTION | No other engine has a top-level anonymous executable block outside a routine definition (T-SQL's<br>nearest analog, a batch, has different scoping/semantics), so a mechanical rewrite risks silently<br>changing what runs when. | [`TestOracleCatalogDropBlock::test_degrades_on_postgresql`](../../tests/integration/test_procedural.<br>py) | Warned limit — preserved as a documented comment below the carrier. |
| <a id="unique-1161"></a>`UNIQUE-1161` | procedural | sp_executesql parameter declarations/bindings dropped; pass them via EXECUTE ... USING manually | T-SQL sp_executesql parameter declarations/bindings (→ MySQL) | MySQL's PREPARE/EXECUTE has no inline parameter-declaration + binding form matching sp_executesql's<br>@params list, so the declarations/bindings are dropped and must be passed via EXECUTE ... USING. | [`ts-sp-executesql`](../../tests/fixtures/challenge/) | Warned limit — parameter bindings dropped. |
| <a id="unique-1162"></a>`UNIQUE-1162` | procedural | notice has no output channel inside a MySQL function; message kept in @uq_notice | A PL/pgSQL RAISE NOTICE inside a function that returns a scalar value (→ MySQL) | A bare SELECT (MySQL's own notice-style output channel) is invalid inside a MySQL FUNCTION —<br>functions cannot return an extra result set (error 1415) — so the message is diverted into a session<br>variable instead. | [`TestMysqlFunctionNotice::test_notice_in_function_diverts`](../../tests/integration/test_pg_source_<br>wave1.py) | Warned limit — the message lands in @uq_notice, not printed inline; procedures (which can SELECT)<br>keep the visible channel. |
| <a id="unique-1163"></a>`UNIQUE-1163` | procedural | original RAISERROR/THROW severity/state args dropped: {rest} | T-SQL RAISERROR/THROW with severity/state arguments (→ MySQL) | MySQL's SIGNAL statement has no severity/state argument slots matching RAISERROR's — only the<br>message transfers, so the extra arguments are dropped and named in the carrier rather than silently<br>discarded. | [`red2-ts-raiserror-format-arg-drop`](../../tests/fixtures/challenge/) | Warned limit — severity/state args dropped, listed in the carrier. |
| <a id="unique-1164"></a>`UNIQUE-1164` | procedural | BEGIN TRANSACTION dropped -- … starts a transaction implicitly | BEGIN TRANSACTION (→ Oracle) | Oracle has no explicit transaction-start statement — a transaction begins implicitly with the first<br>DML statement — so an explicit BEGIN TRANSACTION has nothing to translate to. | [`TestTransactionControl::test_begin_transaction_documented_for_oracle_pg`](../../tests/integration/<br>test_procedural.py) | Faithful (no-op drop) — Oracle's implicit transaction start reproduces the same behaviour. |
| <a id="unique-1165"></a>`UNIQUE-1165` | procedural | WAITFOR TIME '…' has no … equivalent (wait until an absolute time | T-SQL WAITFOR TIME '...' — wait until an absolute clock time (→ MySQL) | Every target's sleep primitive (DBMS_SESSION.SLEEP, pg_sleep, MySQL SLEEP) takes a relative<br>duration, not an absolute wall-clock time to wait until; WAITFOR DELAY (relative) maps cleanly, but<br>WAITFOR TIME has no relative-duration equivalent to compute without the current time at run time. | [`TestWaitFor::test_time_documented`](../../tests/integration/test_procedural.py) | Warned limit — documented, not computed. |
| <a id="unique-1166"></a>`UNIQUE-1166` | procedural | FETCH {direction} has no … equivalent (cursors are forward-only); statement preserved as a comment | A non-forward cursor FETCH (FETCH LAST/PRIOR/FIRST/ABSOLUTE/RELATIVE) from a scrollable cursor (→<br>Oracle) | Oracle cursors, like every other target's, are forward-only (only FETCH NEXT); a non-forward fetch<br>direction has no operation to translate to. | [`TestFetchDirections::test_fetch_last_degrades_oracle`](../../tests/integration/test_pg_source_wave<br>1.py) | Warned limit — the scroll fetch degrades to a carrier; the surrounding OPEN/FETCH NEXT/CLOSE still<br>compile. |
| <a id="unique-1167"></a>`UNIQUE-1167` | procedural | FETCH without INTO — … requires target variables (the source discarded the fetched row); preserved<br>as a comment | A cursor FETCH with no INTO target-variable list | PostgreSQL/Oracle/MySQL all require FETCH to specify where the fetched row's columns go; a source<br>FETCH that discards the row (no INTO) has nothing for the target's FETCH to bind, so emitting 'FETCH<br>c INTO ;' would be invalid syntax. | [`TestUnsupportedCursorConstructsAreValidCarriers::test_fetch_without_into_is_documented_not_empty`]<br>(../../tests/integration/test_procedural.py) | Warned limit — documented as a comment rather than shipping an incomplete FETCH. |
| <a id="unique-1168"></a>`UNIQUE-1168` | procedural | GOTO … dropped -- … has no GOTO; control flow not replicated (docs/03-unsupported.md | GOTO <label> (→ PostgreSQL) | PL/pgSQL has no GOTO statement (or any unconditional-jump control-flow form), so a source GOTO has<br>no operation to translate to. | [`red3-ts-goto-label-proc`](../../tests/fixtures/challenge/) | Warned limit — dropped; control flow is not replicated (T-SQL/Oracle keep native GOTO). |
| <a id="unique-1169"></a>`UNIQUE-1169` | procedural | label … dropped -- … has no GOTO/label (docs/03-unsupported.md | A GOTO target label (→ PostgreSQL) | Without a GOTO to jump to it, and with PL/pgSQL having no label/GOTO mechanism at all, the label<br>marker itself has nothing to bind to on the target. | [`red3-ts-goto-label-proc`](../../tests/fixtures/challenge/) | Warned limit — dropped alongside its GOTO. |
| <a id="unique-1170"></a>`UNIQUE-1170` | procedural | could not translate; preserved for review | _(rationale pending)_ | — | — | — |
| <a id="unique-1171"></a>`UNIQUE-1171` | procedural | procedural statement preserved as a comment; reason carried at runtime | A whole procedural construct the transformer recognizes but cannot map on the target (the shared<br>transformer-degrade carrier) | Shared carrier path for any transformer-level whole-unit degrade (an unsupported PL/SQL exception<br>context in a T-SQL scalar function, a client-tool directive, ...); each specific reason is<br>interpolated into the same carrier template rather than allocating one code per message. | [`pg-named-exception`](../../tests/fixtures/challenge/) | Warned limit — the construct is preserved as a documented comment. |
| <a id="unique-1172"></a>`UNIQUE-1172` | procedural | GOTO … dropped -- MySQL has no GOTO; control flow not replicated (docs/03-unsupported.md | GOTO <label> (→ MySQL) | MySQL has no GOTO statement either; the carrier additionally pairs the drop with a DO 0 no-op so an<br>IF/loop body the GOTO occupied is never left syntactically empty (MySQL error 1064). | [`red3-ts-goto-label-proc`](../../tests/fixtures/challenge/) | Warned limit — dropped; control flow not replicated. |
| <a id="unique-1173"></a>`UNIQUE-1173` | procedural | label … dropped -- MySQL has no GOTO/label (docs/03-unsupported.md | A GOTO target label (→ MySQL) | Same underlying gap as UNIQUE-1169 but for MySQL, which also has no label/GOTO mechanism. | [`red3-ts-goto-label-proc`](../../tests/fixtures/challenge/) | Warned limit — dropped alongside its GOTO. |
| <a id="unique-1174"></a>`UNIQUE-1174` | procedural | Oracle implicit cursor FOR-loop expanded to an explicit MySQL cursor. -- Declare one variable per<br>selected column and complete the FETCH INTO list. DECLARE {done} INT DEFAULT FALSE; DECLARE {cur}<br>CURSOR FOR {cursor_str}; DECLARE CONTINUE HANDLER FOR NOT FOUND SET {done} = TRUE; OPEN {cur};<br>{variable}_loop: LOOP …FETCH {cur} INTO /* col1, col2, ... */; …IF {done} THEN LEAVE<br>{variable}_loop; END IF | An Oracle/PostgreSQL implicit cursor FOR loop whose query's column list Unique cannot resolve (e.g.<br>SELECT * or an unresolvable projection) (→ T-SQL / MySQL) | The faithful expansion needs one FETCH-target variable per selected column; when the column list<br>isn't statically resolvable (a bare SELECT * with no visible schema, or a referenced record field<br>the visible list doesn't expose), Unique cannot generate that variable list, so it emits a<br>documented scaffold for the developer to complete instead of guessing. | [`test_unresolvable_select_star_keeps_documented_scaffold`](../../tests/integration/test_cursor_for_<br>loop_tsql.py) | Warned limit — the FETCH INTO target list is left as a placeholder comment for manual completion;<br>OPEN/CLOSE and the loop shape are still emitted. |
| <a id="unique-1175"></a>`UNIQUE-1175` | procedural | cursor FOR-loop expanded; loop variables are TEXT (exact column types need --db-url metadata). BEGIN | An Oracle/PostgreSQL implicit cursor FOR loop with a resolvable column list (→ MySQL) | The columns are resolvable, so the loop expands completely (one variable per column, positional<br>FETCH), but without a --db-url connection Unique does not know each column's real type, so every<br>loop variable is declared as the permissive TEXT type. | [`TestMySqlCursorForLoopExpansion::test_named_cursor_drives_directly`](../../tests/integration/test_<br>oracle_mysql_tail.py) | Warned limit without --db-url (loop variables are TEXT, not the real column types); the loop's<br>control flow and FETCH are otherwise faithful. |
| <a id="unique-1176"></a>`UNIQUE-1176` | procedural | MySQL has no INSTEAD OF trigger; emitted as BEFORE for review (original was INSTEAD OF, typically on<br>a view). | A T-SQL INSTEAD OF trigger (→ MySQL) | MySQL has no INSTEAD OF trigger timing at all (only BEFORE/AFTER); the closest emulation is a BEFORE<br>trigger, which runs in addition to (not instead of) the triggering statement, so the substitution<br>semantics are not reproduced automatically. | [`TestInsteadOfTrigger::test_instead_of_documented_on_mysql`](../../tests/integration/test_triggers.<br>py) | Warned limit — emitted as BEFORE for review, with the original INSTEAD OF timing documented. |
| <a id="unique-1177"></a>`UNIQUE-1177` | procedural | discarded procedure RETURN value ({val} | A T-SQL procedure-level RETURN <value> (a status code) (→ MySQL) | MySQL stored procedures have no return value (only functions do); a procedure's RETURN <expr> is<br>rewritten to a bare LEAVE (exiting the procedure), and the discarded value is named in the carrier<br>rather than silently dropped. | [`TestReturnValueInProcedure::test_return_value_in_procedure_becomes_leave`](../../tests/integration<br>/test_procedural.py) | Warned limit — the status-code value is documented, not returned; a FUNCTION's RETURN <value> is<br>unaffected and kept. |
| <a id="unique-1178"></a>`UNIQUE-1178` | procedural | dynamic SELECT INTO variable has no direct MySQL form (rewrite the dynamic string to select INTO<br>@session variables); original | A dynamic-SQL EXECUTE ... INTO <var> whose target string is not a compile-time-resolvable SELECT<br>INTO @session-variable form (→ MySQL) | MySQL's PREPARE/EXECUTE workflow can only capture a dynamic result into a variable if the dynamic<br>SQL text itself is rewritten to 'SELECT ... INTO @var', which Unique cannot reliably synthesize for<br>an arbitrary dynamic string built at runtime. | [`pg-dyn-count`](../../tests/fixtures/challenge/) | Warned limit — documented; the dynamic string must be rewritten by hand to select into a MySQL<br>session variable. |
| <a id="unique-1179"></a>`UNIQUE-1179` | procedural | trigger reads the T-SQL inserted/deleted pseudo-tables in a set-based way Oracle cannot express (no<br>transition tables — use a compound trigger); the translation is preserved commented out for a manual<br>rewrite | A trigger body that reads the T-SQL inserted/deleted pseudo-tables in a set-based way (→ Oracle) | Oracle has no transition tables at all (a compound trigger's PL/SQL-collection accumulation is the<br>closest analog, and is not a mechanical rewrite of an arbitrary set-based DML statement), so the<br>set-based read cannot be expressed. | [`TestSetBasedTriggerRewrite::test_pure_set_based_to_oracle_documented`](../../tests/integration/tes<br>t_triggers.py) | Warned limit — the whole trigger degrades to a documented carrier for a manual compound-trigger<br>rewrite (the Oracle-specific sibling of UNIQUE-1155). |
| <a id="unique-1180"></a>`UNIQUE-1180` | procedural | sp_executesql named parameters bind POSITIONALLY here — spell the placeholders inside the dynamic<br>string as :1, :2, … (docs/03-unsupported.md | T-SQL sp_executesql named parameters (→ Oracle) | Oracle EXECUTE IMMEDIATE ... USING binds positionally, so the named @params of sp_executesql must be<br>re-spelled inside the dynamic string as :1, :2, …. | [`ts-sp-executesql`](../../tests/fixtures/challenge/) | Warned limit — placeholders must be renumbered positionally. |
| <a id="unique-1181"></a>`UNIQUE-1181` | procedural | INSTEAD OF trigger aggregates over the inserted/deleted transition table; PostgreSQL INSTEAD OF<br>triggers are row-level only — port by hand (docs/03-unsupported.md | _(rationale pending)_ | — | — | — |
| <a id="unique-1182"></a>`UNIQUE-1182` | procedural | PostgreSQL allows INSTEAD OF only on views; on a table the equivalent is a BEFORE row trigger<br>returning NULL (the original operation is suppressed | A T-SQL INSTEAD OF trigger on a base TABLE, not a view (→ PostgreSQL) | PostgreSQL restricts INSTEAD OF triggers to views only; on a table, the equivalent behaviour<br>(substituting the trigger's own logic for the triggering statement) is a BEFORE row trigger that<br>returns NULL, suppressing the original operation — a different trigger-timing model requiring a<br>pg_trigger_depth() guard so the trigger's own DML still executes. | [`ts-instead-of-insert`](../../tests/fixtures/challenge/) | Faithful — the rewritten BEFORE-trigger-with-guard form reproduces the substitution semantics (live-<br>verified insert-exactly-once). |
| <a id="unique-1183"></a>`UNIQUE-1183` | procedural | BEGIN TRANSACTION dropped -- PostgreSQL manages the routine transaction implicitly | BEGIN TRANSACTION (→ PostgreSQL) | A PL/pgSQL routine already runs inside the caller's transaction (or manages its own via nested<br>procedure-call semantics) — there is no explicit statement to start one, so an explicit BEGIN<br>TRANSACTION has nothing to translate to. | [`TestTransactionControl::test_begin_transaction_documented_for_oracle_pg`](../../tests/integration/<br>test_procedural.py) | Faithful (no-op drop) — PostgreSQL's implicit routine-transaction handling reproduces the same<br>behaviour. |
| <a id="unique-1184"></a>`UNIQUE-1184` | procedural | SAVEPOINT{sp} dropped -- PL/pgSQL has no explicit savepoints; wrap the statements in a BEGIN …<br>EXCEPTION block, which rolls back to its start on error (docs/03-unsupported.md | SAVEPOINT (→ PostgreSQL) | PL/pgSQL has no explicit SAVEPOINT statement; the equivalent behaviour (a partial rollback boundary)<br>comes from wrapping statements in a BEGIN ... EXCEPTION block, which rolls back to its own start on<br>error. | [`TestTransactionControl::test_rollback_to_savepoint`](../../tests/integration/test_procedural.py) | Warned limit — dropped; wrap the guarded statements in a BEGIN...EXCEPTION block to reproduce the<br>rollback boundary (Oracle keeps native SAVEPOINT). |
| <a id="unique-1185"></a>`UNIQUE-1185` | procedural | ROLLBACK TO SAVEPOINT {name} dropped -- PL/pgSQL has no explicit savepoints; the enclosing BEGIN …<br>EXCEPTION block rolls back automatically on error (docs/03-unsupported.md | ROLLBACK TO SAVEPOINT <name> (→ PostgreSQL) | Same underlying gap as UNIQUE-1184 — PL/pgSQL has no explicit savepoints to roll back to; the<br>enclosing BEGIN...EXCEPTION block already rolls back automatically on error. | [`TestTransactionControl::test_rollback_to_savepoint`](../../tests/integration/test_procedural.py) | Warned limit — dropped alongside its SAVEPOINT. |
| <a id="unique-1186"></a>`UNIQUE-1186` | procedural | SELECT * INTO multiple variables needs the column list (no schema to expand '*'); statement<br>preserved as a comment | SELECT * INTO <multiple variables> (→ other engines) | Expanding SELECT * into a positional variable-assignment list requires knowing the source's column<br>list, which a bare '*' does not carry without schema access; the same is true across engines, so the<br>statement cannot be mechanically completed. | [`TestWave233StarIntoMultipleVars::test_star_into_multi_degrades`](../../tests/integration/test_pg_s<br>ource_wave1.py) | Warned limit — documented; supply the column list explicitly to fix. |
| <a id="unique-1187"></a>`UNIQUE-1187` | procedural | cursor FOR-loop expanded; loop variables are NVARCHAR(4000) (exact column types need --db-url<br>metadata). | An Oracle/PostgreSQL implicit cursor FOR loop with a resolvable column list (→ T-SQL) | The T-SQL sibling of UNIQUE-1175 — the columns are resolvable and the loop expands completely (one<br>@variable per column, positional FETCH NEXT), but without --db-url metadata each loop variable is<br>declared as the permissive NVARCHAR(4000) rather than the column's real type. | [`test_named_cursor_loop_expands_completely`](../../tests/integration/test_cursor_for_loop_tsql.py) | Warned limit without --db-url (loop variables are NVARCHAR(4000)); the loop's control flow and FETCH<br>are otherwise faithful and complete. |
| <a id="unique-1188"></a>`UNIQUE-1188` | procedural | SET TRANSACTION {mode} dropped -- T-SQL has no READ ONLY/READ WRITE transaction mode; only ISOLATION<br>LEVEL is expressible (docs/03-unsupported.md | _(rationale pending)_ | — | — | — |
| <a id="unique-1189"></a>`UNIQUE-1189` | procedural | EXECUTE IMMEDIATE USING bindings dropped; inline them or use sp_executesql parameters | _(rationale pending)_ | — | — | — |
| <a id="unique-1190"></a>`UNIQUE-1190` | procedural | verify dynamic SQL placeholders match … | Oracle EXECUTE IMMEDIATE ... USING <binds> with no INTO clause (→ T-SQL) | T-SQL's sp_executesql takes named parameters (@p1, @p2, ...) bound by name, while Oracle's USING<br>binds positionally against :1, :2, ... placeholders inside the dynamic string; the rewrite emits a<br>parameterized sp_executesql call, but the placeholders inside the dynamic-SQL text itself must still<br>be renumbered by hand to match. | [`TestDynamicSQL::test_oracle_to_tsql_uses_sp_executesql`](../../tests/integration/test_procedural.p<br>y) | Warned limit — placeholders inside the dynamic string need manual renumbering to @p1, @p2, ...; the<br>call itself is valid and parameterized. |
| <a id="unique-1191"></a>`UNIQUE-1191` | procedural | OUTPUT <expr> dropped — populate the temp table manually | _(rationale pending)_ | — | — | — |
| <a id="unique-1192"></a>`UNIQUE-1192` | procedural | ROW_COUNT() counts changed rows, not matched rows like the source (docs/03-unsupported.md | Oracle's implicit-cursor SQL%ROWCOUNT (rows the last DML MATCHED) (→ MySQL) | MySQL's closest equivalent, ROW_COUNT(), counts rows actually CHANGED by the last DML, not rows<br>matched by its WHERE clause — for an UPDATE that matches a row but assigns it its current value,<br>Oracle's matched-count and MySQL's changed-count diverge; the mapping is kept (still the closest<br>fit) but annotated rather than shipped silently. | [`TestRowcountDivergenceAnnotation::test_mysql_target_annotates_and_warns`](../../tests/integration/<br>test_challenge.py) | Warned limit — the value may differ from the source when a matched row's UPDATE is a no-op (T-SQL's<br>@@ROWCOUNT is matched-rows too and needs no such note). |
| <a id="unique-1193"></a>`UNIQUE-1193` | procedural | … -- …-only, no … equivalent | A source-only procedural statement with no target concept at all (e.g. T-SQL SET IDENTITY_INSERT/SET<br>NOCOUNT inside a routine) (→ other engines) | The statement configures a source-engine-only session/compiler behaviour with no corresponding<br>concept on the target at all (not merely a missing spelling), so the shared carrier documents it as<br>'{source}-only' rather than attempting any target-side equivalent. | [`TestUniqueCommentRestore::test_identity_insert_documented_then_restored`](../../tests/integration/<br>test_procedural.py) | Warned limit — dropped; documented as source-engine-only, and restored verbatim on a round trip back<br>to the source engine. |
| <a id="unique-1194"></a>`UNIQUE-1194` | procedural | {name} has no … equivalent; {hint} | A T-SQL global (@@ERROR/@@TRANCOUNT/@@CURSOR_ROWS/SQL%ROWCOUNT-family) used inside an expression<br>position (e.g. an IF condition) with no target equivalent | These globals have no faithful non-source equivalent, and unlike a top-level statement, an<br>expression position cannot simply be dropped — a value-shaped placeholder is required so the<br>surrounding expression stays syntactically valid. | [`TestErrorGlobalInCondition::test_error_in_if_mysql`](../../tests/integration/test_procedural.py) | Warned limit — a neutral literal (0) replaces the global so the expression parses; the specific<br>behaviour it gated is lost. |
| <a id="unique-1195"></a>`UNIQUE-1195` | procedural | trigger function … inlined into its T-SQL trigger | A PostgreSQL trigger function's body, when its trigger delegates to it via EXECUTE FUNCTION (→<br>T-SQL) | T-SQL has no separately-callable trigger function — a trigger's logic must live inline in CREATE<br>TRIGGER — so the delegating function's body is inlined directly into the T-SQL trigger, with the PG-<br>only pg_trigger_depth() guard and RETURN NULL protocol dropped (T-SQL triggers have no such re-<br>entrancy-guard convention). | [`TestPgDelegatingTriggerToTSql::test_inlined_into_tsql_trigger`](../../tests/integration/test_trigg<br>ers.py) | Faithful — the inlined trigger reproduces the same DML; live-compiled valid. |
| <a id="unique-1196"></a>`UNIQUE-1196` | procedural | was T-SQL table variable {name} | A T-SQL table variable (DECLARE @var TABLE (...)) (→ MySQL) | MySQL has no table-variable DECLARE form; the closest equivalent is a CREATE TEMPORARY TABLE<br>statement inside the routine body, which the carrier documents as the table variable's replacement. | [`TestTableVariableToMySQL::test_table_variable_becomes_temp_table`](../../tests/integration/test_pr<br>ocedural.py) | Faithful — a temporary table reproduces the same session-scoped, statement-usable storage; the<br>carrier is purely documentary. |
| <a id="unique-1197"></a>`UNIQUE-1197` | procedural | SET option is source-only and has no target equivalent | A source-only SET option with no target equivalent, inside a routine body (e.g. MySQL SET SQL_MODE) | The option configures source-engine-only parsing/execution behaviour (MySQL's SQL_MODE flags, for<br>instance) that no other engine's session model has a matching concept for. | [`TestWave162AdddateSqlMode::test_set_sql_mode_carrier_tsql`](../../tests/integration/test_pg_source<br>_wave1.py) | Warned limit — dropped; documented as source-only. |
| <a id="unique-1198"></a>`UNIQUE-1198` | procedural | T-SQL system procedure has no … equivalent; original: EXEC … | EXEC <T-SQL system procedure> as a standalone statement (e.g. EXEC sp_who) inside a routine body (→<br>other engines) | T-SQL system procedures call into SQL Server's own catalog/admin machinery (the same class as<br>UNIQUE-1211's top-level EXEC, here inside a routine); no other engine exposes the same operation<br>through a callable procedure. | [`ts-waitfor-exec`](../../tests/fixtures/challenge/) | Warned limit — the call becomes a documented carrier; the administrative action must be performed<br>via the target's own tooling. |
| <a id="unique-1199"></a>`UNIQUE-1199` | procedural | T-SQL system procedure has no … equivalent; original: {original} | _(rationale pending)_ | — | — | — |
| <a id="unique-1200"></a>`UNIQUE-1200` | procedural | Oracle package call has no … equivalent; original | An Oracle built-in package call (e.g. DBMS_SCHEDULER.CREATE_JOB) (→ other engines) | Oracle's DBMS_*/UTL_* packages call into Oracle-specific server-side machinery (job scheduling,<br>session control, ...) with no cross-engine equivalent; shipped raw, the call is a guaranteed runtime<br>error off Oracle. | [`test_dbms_scheduler_degrades_to_carrier`](../../tests/integration/test_trigger_predicates_schedule<br>r.py) | Warned limit — the call becomes a documented carrier rather than an invalid raw call. |
| <a id="unique-1201"></a>`UNIQUE-1201` | procedural | trigger uses the T-SQL set-based inserted/deleted pseudo-tables, which have no row-level (NEW/OLD)<br>equivalent. Rewrite manually (PostgreSQL: a statement-level trigger with REFERENCING NEW TABLE AS<br>inserted OLD TABLE AS deleted; Oracle: a compound trigger; MySQL: no transition tables). Original | A trigger DML statement that reads the T-SQL inserted/deleted pseudo-tables in a set-based way (FROM<br>inserted / JOIN deleted) (→ other engines, absent a transition-table rewrite) | A set-based pseudo-table read has no row-level (NEW/OLD) equivalent; where the whole trigger can be<br>rewritten with real transition tables (PostgreSQL statement-level triggers, Oracle compound<br>triggers) the set-based DML is left as-is, but where it cannot (MySQL, or a mixed row+set trigger),<br>the specific statement is documented instead of emitted referencing an undefined table. | [`TestSetBasedTriggerRewrite::test_pure_set_based_to_mysql_documented`](../../tests/integration/test<br>_triggers.py) | Warned limit — the statement is documented with per-target rewrite guidance rather than shipped<br>referencing an undefined table. |
| <a id="unique-1202"></a>`UNIQUE-1202` | procedural | statement uses a table-valued function in FROM, which MySQL does not support; commented out for<br>review | A T-SQL table-valued function used in a FROM clause (→ MySQL) | MySQL has no table-valued function mechanism (a function cannot appear as a FROM-clause row source);<br>the statement is commented out rather than shipping an invalid function-in-FROM. | [`TestTableValuedFunctionInFrom::test_user_tvf_in_from_commented`](../../tests/integration/test_proc<br>edural.py) | Warned limit — commented out for review (STRING_SPLIT, rewritten to the valid JSON_TABLE form, is<br>unaffected). |
| <a id="unique-1203"></a>`UNIQUE-1203` | procedural | unmapped cursor attribute …%… */ (0 = 1 | An Oracle cursor attribute the transformer does not recognize (e.g. c%BULK_ROWCOUNT) (→ other<br>engines) | Only a fixed, explicitly-mapped set of cursor attributes (%FOUND/%NOTFOUND/%ISOPEN/%ROWCOUNT) has a<br>per-cursor state translation on T-SQL/MySQL; an attribute outside that set has no known target form,<br>and — critically — must not fall through to the general expression parser, which would otherwise<br>read 'c%attr' as 'c' modulo 'attr'. | [`TestUnknownAttributeWarns::test_unknown_attribute_warns_and_does_not_leak_modulo`](../../tests/int<br>egration/test_cursor_state_b7.py) | Warned limit — degrades to a neutral 0 carrier rather than silently becoming modulo arithmetic. |
| <a id="unique-1204"></a>`UNIQUE-1204` | procedural | no MySQL equivalent: ALTER TRIGGER … … | _(rationale pending)_ | — | — | — |
| <a id="unique-1205"></a>`UNIQUE-1205` | procedural | was T-SQL temp table #{var} | A T-SQL #temp table declared with an explicit CREATE TABLE #name (...) (→ Oracle) | Same underlying gap as UNIQUE-1196 but for a real temp table rather than a table variable — Oracle<br>has no session-scoped #temp table; the CREATE is hoisted to a GLOBAL TEMPORARY TABLE before the<br>routine (a CREATE cannot live inside PL/SQL), and the carrier documents the substitution. | [`TestOracle::test_hoists_global_temporary_table`](../../tests/integration/test_temp_table_in_proced<br>ure.py) | Faithful — the hoisted GTT, cleared and repopulated per call, reproduces the same session-scoped<br>storage; the carrier is purely documentary. |
| <a id="unique-1206"></a>`UNIQUE-1206` | procedural | {word} dropped -- the exception-guarded block is a subtransaction (transaction control there is a<br>runtime error); it rolls back on error and commits with the surrounding transaction | COMMIT/ROLLBACK inside a T-SQL TRY/CATCH block translated to a PL/pgSQL BEGIN...EXCEPTION block (→<br>PostgreSQL) | PL/pgSQL's exception-guarded block is itself a subtransaction (savepoint); issuing an explicit<br>COMMIT/ROLLBACK inside one is a runtime error ('cannot commit while a subtransaction is active')<br>rather than a parse-time gap, and the subtransaction already provides the same rollback-on-<br>error/commit-with-caller semantics T-SQL's TRY/CATCH expressed explicitly. | [`TestTopLevelTryCatch::test_begin_transaction_prefix_lowers_on_postgresql`](../../tests/integration<br>/test_procedural.py) | Faithful (the subtransaction reproduces the same net transactional behaviour) with the explicit<br>COMMIT/ROLLBACK dropped and documented rather than shipped as a guaranteed runtime error. |
| <a id="unique-1207"></a>`UNIQUE-1207` | orchestration | approved value divergence (collation/encoding) kept with a warning; reason carried at runtime | Inherent value divergence: default-collation comparison, Oracle '' ≡ NULL, or byte-vs-char length<br>(approved limit) | These divergences (case/accent/trailing-space comparison under the default collation, Oracle's '' ≡<br>NULL, LENGTH byte-vs-char) are per-column/connection properties the SQL text carries no trace of; no<br>statement-level rewrite bridges them without column-collation/encoding visibility Unique does not<br>have. | [`ora-empty-null`](../../tests/fixtures/challenge/) | Approved documented limit, warned — the value or row count may differ. |
| <a id="unique-1208"></a>`UNIQUE-1208` | orchestration | T-SQL CREATE SCHEMA has no Oracle equivalent — an Oracle schema is a database user. Create it<br>manually, e.g. CREATE USER {name} …; original | CREATE SCHEMA (T-SQL) → Oracle | Oracle has no CREATE SCHEMA statement — a schema on Oracle IS a database user (created with CREATE<br>USER and granted privileges), a fundamentally different object model from T-SQL's schema as a<br>namespace within a shared database. | [`TestTranspiler::test_create_schema_oracle_documented_carrier`](../../tests/unit/core/test_transpil<br>er.py) | Warned limit — documented as unsupported rather than emitting an invalid CREATE SCHEMA; create the<br>equivalent Oracle user by hand. |
| <a id="unique-1209"></a>`UNIQUE-1209` | orchestration | Oracle ORGANIZATION INDEX/HEAP is a physical-storage clause with no equivalent here; dropped. | Oracle ORGANIZATION INDEX/HEAP table-organization clause (→ PostgreSQL/T-SQL/MySQL) | ORGANIZATION INDEX/HEAP selects Oracle's physical row-storage strategy (index-organized vs heap-<br>organized table) — a storage-engine-level choice with no logical-schema meaning and no equivalent<br>concept on any other engine. | [`TestCrossDialectDDL::test_oracle_organization_index_table_converted`](../../tests/integration/test<br>_cross_dialect.py) | Faithful in result (storage-only); the clause is dropped and the table converts as an ordinary<br>table. |
| <a id="unique-1210"></a>`UNIQUE-1210` | orchestration | … -- tsql-only, no {target} equivalent (constraint check-state | ALTER TABLE t {CHECK\|NOCHECK} CONSTRAINT c — a constraint's enabled/disabled check-state toggle<br>(T-SQL) → MySQL | T-SQL's CHECK/NOCHECK CONSTRAINT toggles whether an existing constraint is currently enforced<br>without dropping it; MySQL has no equivalent enable/disable toggle for a constraint (Oracle maps to<br>ENABLE/DISABLE CONSTRAINT, PostgreSQL to VALIDATE CONSTRAINT) — the state change itself has nothing<br>to become. | [`TestTranspiler::test_constraint_check_state_toggle`](../../tests/unit/core/test_transpiler.py) | Warned limit — preserved as a restorable note rather than dropped; the constraint's enforcement<br>state on MySQL is unchanged. |
| <a id="unique-1211"></a>`UNIQUE-1211` | orchestration | {sp} is a SQL Server system procedure with no {target} equivalent; original call omitted | EXEC sp_<name> — a T-SQL system procedure (→ other engines) | T-SQL system procedures call SQL Server's own catalog/admin machinery; no other engine exposes the<br>same operation through a callable procedure with the same name or signature. | [`reda-ts-exec-swallow-next`](../../tests/fixtures/challenge/) | Warned limit — the call becomes a carrier; the administrative action must be performed via the<br>target's own tooling. |
| <a id="unique-1212"></a>`UNIQUE-1212` | orchestration | {target} has no standalone OUTPUT/RETURNING result set; the statement returned: {cols}<br>(docs/03-unsupported.md | A standalone INSERT/UPDATE/DELETE ... OUTPUT result set (→ Oracle / MySQL) | Neither Oracle (RETURNING is PL/SQL-only, ORA-63809) nor MySQL has a standalone data-modifying-<br>statement result set, so the OUTPUT rows cannot be returned to the caller. | [`ts-insert-output`](../../tests/fixtures/challenge/) | Warned limit — the DML effect is faithful; the returned result set is documented, not produced. |
| <a id="unique-1213"></a>`UNIQUE-1213` | orchestration | T-SQL default constraint value has no {target} equivalent | _(rationale pending)_ | — | — | — |
| <a id="unique-1214"></a>`UNIQUE-1214` | orchestration | READ COMMITTED is Oracle's default isolation level (no-op; noted so a following SET TRANSACTION mode<br>statement can still open the transaction | _(rationale pending)_ | — | — | — |
| <a id="unique-1215"></a>`UNIQUE-1215` | orchestration | T-SQL has no SET ROLE (use role membership / EXECUTE AS); statement preserved as a comment. | SET ROLE (PostgreSQL/MySQL/Oracle) → T-SQL | SET ROLE changes the current session's active role/privilege set on the engines that have a role<br>system; T-SQL has no SET ROLE statement at all — role-like membership is expressed through EXECUTE<br>AS or role membership grants instead, a structurally different mechanism. | [`TestWave139DecodeAndSetRole::test_set_role_degrades_tsql`](../../tests/integration/test_pg_source_<br>wave1.py) | Warned limit — statement preserved as a comment; use role membership / EXECUTE AS on T-SQL instead. |
| <a id="unique-1216"></a>`UNIQUE-1216` | orchestration | {target} has no deferred-constraint toggling (SET CONSTRAINTS); statement preserved as a comment. | SET CONSTRAINTS ... {DEFERRED\|IMMEDIATE} (PostgreSQL/Oracle) → MySQL/T-SQL | SET CONSTRAINTS toggles when a DEFERRABLE constraint's check runs (at each statement vs at COMMIT);<br>MySQL and T-SQL have no deferrable-constraint model at all, so there is no timing to toggle. | [`TestZeroPushPgOnlyShapes::test_set_constraints_carrier`](../../tests/unit/core/test_ir_first_famil<br>ies.py) | Warned limit — statement preserved as a comment. |
| <a id="unique-1217"></a>`UNIQUE-1217` | orchestration | SET SESSION AUTHORIZATION has no {target} equivalent; switch users natively. | SET SESSION AUTHORIZATION (PostgreSQL) → T-SQL/MySQL/Oracle | SET SESSION AUTHORIZATION switches the session's effective user for privilege checks — a PostgreSQL-<br>specific session directive; the other engines switch users through entirely different mechanisms<br>(T-SQL EXECUTE AS, MySQL/Oracle connection-level authentication), none of which is a drop-in SQL-<br>statement substitution. | [`TestSessionAuthorizationDegrades::test_session_authorization_degrades`](../../tests/integration/te<br>st_pg_source_wave1.py) | Warned limit — statement preserved as a comment; switch users through the target's own mechanism. |
| <a id="unique-1218"></a>`UNIQUE-1218` | orchestration | PostgreSQL session setting has no {target} equivalent; configure the session natively. | A PostgreSQL session GUC, e.g. SET extra_float_digits = 0 / SET x TO v / RESET x (→<br>T-SQL/MySQL/Oracle) | PostgreSQL's SET/RESET cover hundreds of engine-internal session tuning knobs (query planner costs,<br>timeouts, locale, ...) with no cross-engine namespace at all — each is either meaningless or<br>configured through a completely different mechanism on the other three engines. | [`TestPgGucSettings::test_guc_assignment_degrades`](../../tests/integration/test_pg_source_wave1.py) | Warned limit — statement preserved as a comment; configure the session natively on the target. |
| <a id="unique-1219"></a>`UNIQUE-1219` | orchestration | MySQL session setting has no {target} equivalent; configure the session natively. | A MySQL session knob, e.g. SET sql_mode = '...' / FLUSH STATUS (→ PostgreSQL/T-SQL/Oracle) | MySQL's SET (bare name, GLOBAL/SESSION/PERSIST) and its admin statements (FLUSH/LOCK TABLES/ANALYZE<br>TABLE/...) are engine-local session and maintenance knobs with no meaning on any other engine. | [`TestMysqlSessionKnobsDegrade::test_knob_degrades`](../../tests/integration/test_pg_source_wave1.py<br>) | Warned limit — statement preserved as a comment; configure the session / run maintenance natively on<br>the target. |
| <a id="unique-1220"></a>`UNIQUE-1220` | orchestration | live {target} validation rejected this statement ({first_err}); preserved as a comment | _(rationale pending)_ | — | — | — |
| <a id="unique-1221"></a>`UNIQUE-1221` | orchestration | T-SQL TEXTIMAGE_ON filegroup clause dropped (physical storage, no logical-schema impact) | TEXTIMAGE_ON <filegroup> — a T-SQL LOB-storage-placement clause (→ PostgreSQL/MySQL/Oracle) | TEXTIMAGE_ON pins a table's LOB columns to a specific T-SQL filegroup — a physical storage-placement<br>detail with no logical-schema meaning and no equivalent concept (filegroups themselves are T-SQL-<br>only) on any other engine. | [`TestTranspiler::test_textimage_on_filegroup_stripped`](../../tests/unit/core/test_transpiler.py) | Faithful in result (storage-only); the clause is dropped so the CREATE TABLE converts instead of<br>falling back to an unparsed passthrough. |
| <a id="unique-1222"></a>`UNIQUE-1222` | orchestration | T-SQL WITH NOCHECK dropped; the constraint is added and the target validates existing rows (no<br>NOVALIDATE applied) | ALTER TABLE t WITH NOCHECK ADD CONSTRAINT ... (T-SQL) → PostgreSQL/MySQL/Oracle | T-SQL's WITH NOCHECK adds a constraint without validating existing rows against it; the other<br>engines either validate immediately with no opt-out (MySQL/Oracle) or validate immediately by<br>default with a different deferred-validation syntax entirely (PostgreSQL's NOT VALID, a separate<br>construct). | [`TestTranspiler::test_alter_add_constraint_with_nocheck_stripped`](../../tests/unit/core/test_trans<br>piler.py) | Warned limit — the constraint is added and the target validates existing rows immediately, unlike<br>the source's NOCHECK. |
| <a id="unique-1223"></a>`UNIQUE-1223` | orchestration | session/client directive commented out (no cross-engine equivalent); the directive is session-scoped<br>and the specific statement is carried at runtime | A SQL*Plus client directive, e.g. SET SERVEROUTPUT ON (Oracle) → PostgreSQL/T-SQL/MySQL | SQL*Plus SET directives configure the CLIENT tool's display behavior — they have no server-side<br>meaning even on Oracle itself, let alone on another engine's SQL grammar, where they are a syntax<br>error. | [`test_representative_warnings_are_coded`](../../tests/unit/core/test_diagnostic_completeness.py) | Faithful (no server-side effect anywhere) — the directive is commented out rather than shipped as<br>invalid SQL. |
| <a id="unique-1224"></a>`UNIQUE-1224` | orchestration | batch commented out (unrecognized migration-guard shape); the specific batch is carried at runtime | _(rationale pending)_ | — | — | — |
| <a id="unique-1225"></a>`UNIQUE-1225` | statement | existence guard dropped; the guarded statement now runs unconditionally (no conditional form on the<br>target); the specific statement is carried at runtime | An unrecognized IF <catalog-guard> BEGIN ... END migration-guard batch that no shape-recognizer<br>models | Idempotent-migration IF-guards (IF [NOT] EXISTS(...) around a DDL/DML body) are recognized and<br>rewritten only for the shapes the guard-translation layer models; a guard body that falls outside<br>every modeled shape (e.g. an unsupported statement inside it) cannot be safely rewritten, so it is<br>preserved whole rather than guessed at. | [`TestHonestFallbackLabel::test_non_set_batch_gets_honest_signal`](../../tests/unit/core/test_guard_<br>translation.py) | Warned limit — the guarded statement now runs unconditionally (the guard is dropped); the original<br>is carried in the comment. |
| <a id="unique-1226"></a>`UNIQUE-1226` | statement | guard ELSE branch dropped (only a diagnostic PRINT can be carried into the target conditional); the<br>specific branch is carried at runtime | An IF <real-data condition> ... ELSE ... guard whose ELSE branch is not a bare diagnostic PRINT | The IF/ELSE guard rewrite (e.g. into PostgreSQL's DO $$ ... IF ... THEN ... END IF; END $$) can<br>carry a PRINT-only ELSE body into the target's own diagnostic-output statement, but an ELSE with<br>real DML/DDL has no such narrow, safe rewrite — dropping vs. keeping it both risk changing which<br>branch runs, so it is flagged rather than guessed at. | [`TestGuardElseBranch::test_non_print_else_warns`](../../tests/unit/core/test_guard_translation.py) | Warned limit — only the THEN branch is translated; the ELSE branch is dropped and carried in the<br>comment. |
| <a id="unique-1227"></a>`UNIQUE-1227` | ddl | Oracle MODIFY keeps the column's current nullability; the redundant NULL is omitted (an explicit<br>NULL raises ORA-01451 when the column is already nullable) | _(rationale pending)_ | — | — | — |
| <a id="unique-1228"></a>`UNIQUE-1228` | validation | internal: a parsed sqlglot construct was not consumed by the converter (unread arg) — the construct<br>may be dropped; the specific arg is carried at runtime | _(rationale pending)_ | — | — | — |
| <a id="unique-1229"></a>`UNIQUE-1229` | validation | DML transpilation failed (internal error); the source statement is preserved as a comment; the error<br>is carried at runtime | _(rationale pending)_ | — | — | — |
| <a id="unique-1230"></a>`UNIQUE-1230` | procedural | procedural parse note; the specific reason is carried at runtime | _(rationale pending)_ | — | — | — |
| <a id="unique-1231"></a>`UNIQUE-1231` | procedural | procedural transformation note; the specific reason is carried at runtime | Any ProceduralTransformer-level warning with no more specific UNIQUE-NNNN code of its own | The shared fallback code for procedural-transform warnings — most transform-level messages carry<br>their own specific code (reconciled from the matching inline carrier in the output), but a message<br>with no corresponding inline carrier still needs a stable code to report through rather than<br>shipping uncoded. | [`TestParseFallbackDegradesCrossDialect::test_unparsed_routine_degrades_pg`](../../tests/integration<br>/test_pg_source_wave1.py) | Varies by the underlying message; in the bound example, a MySQL CONTINUE handler for SQLEXCEPTION<br>has no PostgreSQL equivalent and the whole routine degrades to a documented carrier. |
| <a id="unique-1232"></a>`UNIQUE-1232` | procedural | procedural transpilation failed (internal error); the routine is preserved; the error is carried at<br>runtime | _(rationale pending)_ | — | — | — |
| <a id="unique-1233"></a>`UNIQUE-1233` | statement | transaction closer preserved as a comment: its opener degraded to a parse-failure carrier, so<br>shipping the COMMIT/ROLLBACK would orphan it (no open transaction — T-SQL error 3902) | A transaction closer (COMMIT/END/ROLLBACK) whose opener failed | When a transaction opener (BEGIN) glues to the next statement and fails to parse, that whole batch<br>degrades to a parse-failure carrier — no BEGIN reaches the output. Emitting the sibling closer as an<br>executable COMMIT/ROLLBACK would then run against no open transaction (T-SQL error 3902), so the<br>closer must degrade too. | [`TestTransactionOpenerDegradeCoherence::test_orphan_closer_after_failed_opener_degrades`](../../tes<br>ts/unit/core/test_transpiler.py) | Coherent degrade — the closer is preserved as a comment so the output has no orphan COMMIT; both<br>halves of the broken transaction unit are carried, not silently dropped. |

233 codes across 6 categories.
