# Degradation catalog ([limit] cases)

> **Generated — do not edit by hand.** Produced by `python scripts/generate_reference_docs.py` from every `-- CASE[limit]...:` header in `tests/fixtures/challenge/challenge_*.sql`. The CI freshness gate (`python scripts/generate_reference_docs.py --check`) fails the build if this file drifts from the source data.

Each row is an approved, documented divergence: a construct the transpiler cannot render faithfully off the listed source engine, already live-verified and citing its [docs/03-unsupported.md](../03-unsupported.md) entry. A future stable per-case code (`UNIQUE-NNNN`, brief B32) will slot in as an additional column once that registry lands.

| Case ID | Source | Class | Description | 03-unsupported |
|---|---|---|---|---|
| `my-accent-eq` | mysql | — | fails on oracle, postgresql, tsql. APPROVED LIMIT (2026-07-18): collation case/accent/trailing-space<br>sensitivity is a per-column property, not statement-compensable (docs/03-unsupported.md §2). FUNC-<br>DIFF: source=(('1',),) target=(('0',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-agg-bit` | mysql | — | fails on oracle, tsql. BIT_AND/BIT_OR/BIT_XOR are aggregates on MySQL & PostgreSQL (transpiled<br>faithfully, value-verified) but Oracle and T-SQL have no bit-aggregate function<br>(docs/03-unsupported.md §3.10). Warned carrier on oracle/tsql. | [03-unsupported.md §3.10](../03-unsupported.md) |
| `my-agg-collect` | mysql | — | JSON_ARRAYAGG maps to PostgreSQL json_agg (works, live-verified [1,2]); T-SQL has no JSON aggregate,<br>so its output degrades to a carrier + warning (docs/03-unsupported.md). fails on tsql | [03-unsupported.md](../03-unsupported.md) |
| `my-any-value` | mysql | — | ANY_VALUE + GROUP_CONCAT map to PostgreSQL (ANY_VALUE is native in PG16+, GROUP_CONCAT->STRING_AGG;<br>works, live-verified (1,'1,2')); T-SQL has no ANY_VALUE, so its output degrades to a carrier +<br>warning (docs/03-unsupported.md). fails on tsql | [03-unsupported.md](../03-unsupported.md) |
| `my-bit-agg` | mysql | — | fails on oracle, tsql. BIT_XOR/BIT_OR aggregates map faithfully MySQL<->PostgreSQL; Oracle/T-SQL<br>have no bit aggregate (docs/03-unsupported.md §3.10). Warned carrier on oracle/tsql. | [03-unsupported.md §3.10](../03-unsupported.md) |
| `my-bit-char-len` | mysql | — | fails on postgresql. APPROVED LIMIT (2026-07-18): LENGTH/BIT_LENGTH byte-vs-char, encoding-dependent<br>(docs/03-unsupported.md §2). FUNC-DIFF: source=(('24', '1', '3'),) target=(('24', '1', '1'),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-bit-count` | mysql | — | BIT_COUNT (population count) has no cross-engine builtin; gated + annotated<br>(docs/03-unsupported.md). fails on oracle, postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `my-bit-fns` | mysql | — | BIT_COUNT (population count) has no cross-engine builtin; gated + annotated<br>(docs/03-unsupported.md). fails on postgresql | [03-unsupported.md](../03-unsupported.md) |
| `my-bit-negative` | mysql | — | fails on oracle, postgresql, tsql. MySQL treats bitwise operands as unsigned 64-bit, so ~ and<br>negative-operand bit ops diverge from signed engines. No faithful mapping (docs/03-unsupported.md). | [03-unsupported.md](../03-unsupported.md) |
| `my-bitnot` | mysql | — | fails on oracle, postgresql, tsql. MySQL bitwise NOT is unsigned 64-bit (~0=18446744073709551615);<br>other engines are signed (-1). No faithful unsigned-64 type (docs/03-unsupported.md). | [03-unsupported.md](../03-unsupported.md) |
| `my-bitnot-arith` | mysql | — | fails on oracle, postgresql, tsql. MySQL bitwise ops are unsigned 64-bit, so ~5+1 overflows into a<br>big unsigned value vs a signed -5 elsewhere. No faithful unsigned-64 mapping<br>(docs/03-unsupported.md). | [03-unsupported.md](../03-unsupported.md) |
| `my-bitops` | mysql | — | fails on oracle, postgresql, tsql. MySQL bitwise NOT/shift are unsigned 64-bit<br>(~5=18446744073709551610); other engines signed. The high-bit results diverge<br>(docs/03-unsupported.md). | [03-unsupported.md](../03-unsupported.md) |
| `my-cast-convert` | mysql | — | CAST AS UNSIGNED has no signed-engine type; mapped to NUMERIC/NUMBER (value 1 exact) with a carrier<br>flagging the lost unsigned wraparound (docs/03-unsupported.md). fails on oracle, postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `my-cast-datetime2` | mysql | — | the CAST(... AS TIME) part has no Oracle type; kept as text with a documented carrier<br>(docs/03-unsupported.md). fails on oracle | [03-unsupported.md](../03-unsupported.md) |
| `my-cast-json` | mysql | — | MySQL's JSON type has no faithful cross-engine cast (T-SQL has no JSON type at all; MySQL's<br>canonical JSON spacing '[1, 2]' differs from PG/Oracle), so a CAST to JSON keeps the value as text +<br>annotation (docs/03-unsupported.md). fails on oracle, postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `my-cast-time` | mysql | — | Oracle has no TIME type; CAST(... AS TIME) keeps the value as text with a documented carrier<br>(docs/03-unsupported.md). fails on oracle | [03-unsupported.md](../03-unsupported.md) |
| `my-char-256` | mysql | — | MySQL CHAR(n) is byte-based (CHAR(256) = the 2-byte string 0x0100), not a single code point like<br>CHR; carrier + warning (docs/03-unsupported.md). fails on oracle, postgresql | [03-unsupported.md](../03-unsupported.md) |
| `my-coalesce-empty` | mysql | — | fails on oracle. Oracle stores '' as NULL (docs/03-unsupported.md). FUNC-DIFF: source=(('1',),)<br>target=(('NULL',),) | [03-unsupported.md](../03-unsupported.md) |
| `my-collation-fn` | mysql | — | COLLATION(x) returns engine-specific collation names (MySQL utf8mb4_0900_ai_ci vs Oracle<br>USING_NLS_COMP); the function exists on both but can't match — carrier + warning<br>(docs/03-unsupported.md). fails on oracle | [03-unsupported.md](../03-unsupported.md) |
| `my-convert-tz` | mysql | — | CONVERT_TZ has no uniform cross-engine form (Oracle needs format-aware parsing + FROM_TZ, T-SQL AT<br>TIME ZONE takes named Windows zones not offsets, and the result type/format differ); it gates +<br>annotates on every target (docs/03-unsupported.md). fails on oracle, postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `my-date-diff-minus` | mysql | — | MySQL DATE - DATE is a numeric YYYYMMDD subtraction (200), not a day count; the meaningful day count<br>(60) is emitted with a documented carrier (docs/03-unsupported.md). fails on oracle, postgresql | [03-unsupported.md](../03-unsupported.md) |
| `my-dateformat-iso` | mysql | — | fails on oracle, postgresql, tsql. date format mask uses a bare-letter literal / locale name /<br>exotic token that cannot round-trip to a quoted cross-engine mask (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `my-dateformat-long` | mysql | — | fails on oracle, postgresql, tsql. date format mask uses a bare-letter literal / locale name /<br>exotic token that cannot round-trip to a quoted cross-engine mask (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `my-distinct-case` | mysql | — | fails on postgresql, oracle. MySQL's default collation is case-insensitive so DISTINCT collapses<br>'a'='A' (2 rows); PG/Oracle are case-sensitive (3 rows), no portable equivalent<br>(docs/03-unsupported.md §3.14). T-SQL is also CI (2 rows, collation-equal representative). | [03-unsupported.md §3.14](../03-unsupported.md) |
| `my-empty-eq-zero` | mysql | — | fails on oracle. Oracle stores '' as NULL (docs/03-unsupported.md). FUNC-DIFF: source=(('1',),)<br>target=(('NULL',),) | [03-unsupported.md](../03-unsupported.md) |
| `my-eq-mix` | mysql | — | fails on oracle, tsql. APPROVED LIMIT (2026-07-18): collation case/accent/trailing-space sensitivity<br>is a per-column property, not statement-compensable (docs/03-unsupported.md §2). FUNC-DIFF:<br>source=(('1', '0', '1'),) target=(('1', '1', '1'),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-fcollate` | mysql | — | fails on oracle, postgresql. APPROVED LIMIT (2026-07-18): collation case/accent/trailing-space<br>sensitivity is a per-column property, not statement-compensable (docs/03-unsupported.md §2). FUNC-<br>DIFF: source=(('c', 'a', '1'),) target=(('c', 'B', '0'),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-flen` | mysql | — | fails on oracle, postgresql, tsql. APPROVED LIMIT (2026-07-18): LENGTH/BIT_LENGTH byte-vs-char,<br>encoding-dependent (docs/03-unsupported.md §2). FUNC-DIFF: source=(('5', '4', '6', '2'),)<br>target=(('4', '4', '2', '2'),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-fmt-spec` | mysql | — | fails on oracle. date format mask uses a bare-letter literal / locale name / exotic token that<br>cannot round-trip to a quoted cross-engine mask (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `my-fmt-spec2` | mysql | — | fails on oracle, postgresql, tsql. date format mask uses a bare-letter literal / locale name /<br>exotic token that cannot round-trip to a quoted cross-engine mask (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `my-fmt3` | mysql | — | fails on oracle, postgresql, tsql. FORMAT with a locale (de_DE) has no cross-engine equivalent<br>(docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `my-for-share` | mysql | — | FOR SHARE (a shared row lock) has no Oracle equivalent (Oracle SELECT locking is FOR UPDATE,<br>exclusive); the shared lock is dropped and the divergence annotated (docs/03-unsupported.md). fails<br>on oracle | [03-unsupported.md](../03-unsupported.md) |
| `my-fsubstr` | mysql | — | SUBSTRING edge folds now emit MySQL's exact values ('', from-end, clamp; live-verified on pg/tsql);<br>the residual divergence is Oracle's ''≡NULL (no faithful workaround), annotated + warned per<br>docs/03-unsupported.md. fails on oracle | [03-unsupported.md](../03-unsupported.md) |
| `my-fulltext` | mysql | — | fails on oracle, postgresql, tsql. MySQL FULLTEXT index + MATCH()..AGAINST() has no faithful cross-<br>engine equivalent (Oracle Text, PG tsvector/GIN, T-SQL full-text catalog are all different engines<br>with different syntax) (docs/03-unsupported.md §2). Warned carrier on all three. | [03-unsupported.md §2](../03-unsupported.md) |
| `my-group-case` | mysql | — | GROUP BY over case-variant values: MySQL's CI default collation groups 'a'/'A' as one key; CS<br>targets keep two (and even CI T-SQL picks an arbitrary representative). No statement-level fix —<br>annotated + warned via the case-variant-literal rule (docs/03-unsupported.md §3.14). fails on<br>oracle, postgresql, tsql | [03-unsupported.md §3.14](../03-unsupported.md) |
| `my-ifnull-empty` | mysql | — | fails on oracle. Oracle stores '' as NULL, so an empty-string result cannot survive<br>(docs/03-unsupported.md). FUNC-DIFF: source=(('',),) target=(('NULL',),) | [03-unsupported.md](../03-unsupported.md) |
| `my-int-or-empty` | mysql | — | fails on oracle. Oracle stores '' as NULL (docs/03-unsupported.md). FUNC-DIFF: source=(('0',),)<br>target=(('NULL',),) | [03-unsupported.md](../03-unsupported.md) |
| `my-json-agg` | mysql | — | fails on tsql. JSON_ARRAYAGG/JSON_OBJECTAGG map faithfully across MySQL, PostgreSQL<br>(json_agg/json_object_agg) and Oracle (value-verified); T-SQL has no JSON aggregate<br>(docs/03-unsupported.md §3.9). Warned carrier on tsql. | [03-unsupported.md §3.9](../03-unsupported.md) |
| `my-json-aggs` | mysql | — | fails on tsql. Same as my-json-agg (JSON_ARRAYAGG/JSON_OBJECTAGG faithful MySQL<->PG<->Oracle);<br>T-SQL has no JSON aggregate (docs/03-unsupported.md §3.9). Warned carrier on tsql. | [03-unsupported.md §3.9](../03-unsupported.md) |
| `my-json-merge` | mysql | — | MySQL JSON_MERGE_PATCH (RFC 7396 deep merge) has no portable form (Oracle spells it JSON_MERGEPATCH,<br>PG has only shallow \|\|); catalog-gated + annotated (docs/03-unsupported.md). fails on oracle,<br>postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `my-length-bytes` | mysql | — | fails on oracle, postgresql, tsql. APPROVED LIMIT (2026-07-18): LENGTH bytes-vs-chars, encoding-<br>dependent, not statement-compensable (docs/03-unsupported.md §2). FUNC-DIFF: source=(('5',),)<br>target=(('4',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-length-div` | mysql | — | MySQL LENGTH() counts bytes and the division result's representation differs across engines;<br>annotated bytes-vs-chars divergence (docs/03-unsupported.md). fails on oracle, tsql | [03-unsupported.md](../03-unsupported.md) |
| `my-length-unicode` | mysql | — | fails on oracle, postgresql, tsql. APPROVED LIMIT (2026-07-18): LENGTH bytes-vs-chars, encoding-<br>dependent, not statement-compensable (docs/03-unsupported.md §2). FUNC-DIFF: source=(('5', '4',<br>'5'),) target=(('4', '4', '3'),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-like-ci` | mysql | — | fails on oracle, postgresql. APPROVED LIMIT (2026-07-18): collation case/accent/trailing-space<br>sensitivity is a per-column property, not statement-compensable (docs/03-unsupported.md §2). FUNC-<br>DIFF: source=(('1',),) target=(('0',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-like-single` | mysql | — | fails on oracle, postgresql. APPROVED LIMIT (2026-07-18): LIKE case-sensitivity is a default-<br>collation property, not statement-compensable (docs/03-unsupported.md §2). FUNC-DIFF:<br>source=(('1',),) target=(('0',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-order-strings` | mysql | — | ORDER BY over case-variant values under MySQL's CI collation: CS targets sort 'banana'/'Banana'<br>apart and equal-key tie order is unspecified. Annotated + warned via the case-variant-literal rule<br>(docs/03-unsupported.md §3.14). fails on oracle, postgresql, tsql | [03-unsupported.md §3.14](../03-unsupported.md) |
| `my-self-fk` | mysql | — | T-SQL forbids a cascading action on a self-referencing FK (error 1785): downgraded to ON DELETE NO<br>ACTION + warned note (emulate with an AFTER trigger if needed) (docs/03-unsupported.md §3.22). fails<br>on tsql | [03-unsupported.md §3.22](../03-unsupported.md) |
| `my-str-lt` | mysql | — | fails on oracle, postgresql. APPROVED LIMIT (2026-07-18): collation case/accent/trailing-space<br>sensitivity is a per-column property, not statement-compensable (docs/03-unsupported.md §2). FUNC-<br>DIFF: source=(('1',),) target=(('0',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-trailing-eq` | mysql | — | fails on oracle, tsql. APPROVED LIMIT (2026-07-18): collation case/accent/trailing-space sensitivity<br>is a per-column property, not statement-compensable (docs/03-unsupported.md §2). FUNC-DIFF:<br>source=(('0',),) target=(('1',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-trailing-space-cmp` | mysql | — | fails on oracle, postgresql, tsql. APPROVED LIMIT (2026-07-18): collation case/accent/trailing-space<br>sensitivity is a per-column property, not statement-compensable (docs/03-unsupported.md §2). FUNC-<br>DIFF: source=(('0', '1', '1'),) target=(('1', '0', '1'),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-tz-convert` | mysql | — | CONVERT_TZ with a named IANA zone (America/New_York) has no faithful cross-engine equivalent (T-SQL<br>uses Windows zone names, and DST rules differ), so the whole statement is gated + annotated<br>(docs/03-unsupported.md). fails on oracle, postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `my-upper-sharps` | mysql | — | fails on postgresql. APPROVED LIMIT (2026-07-18): non-ASCII case-folding (ß, accents) is<br>locale/collation-dependent, not statement-compensable (docs/03-unsupported.md §2). FUNC-DIFF:<br>source=(('ß',),) target=(('ẞ',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-upper-sharps-len` | mysql | — | fails on oracle, postgresql, tsql. APPROVED LIMIT (2026-07-18): non-ASCII case-folding (ß, accents)<br>is locale/collation-dependent, not statement-compensable (docs/03-unsupported.md §2). FUNC-DIFF:<br>source=(('2',),) target=(('1',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-upper-strasse` | mysql | — | fails on postgresql. APPROVED LIMIT (2026-07-18): non-ASCII case-folding (ß, accents) is<br>locale/collation-dependent, not statement-compensable (docs/03-unsupported.md §2). FUNC-DIFF:<br>source=(('STRAßE',),) target=(('STRAẞE',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `my-xml-fns` | mysql | — | ExtractValue translates per engine (Oracle EXTRACTVALUE(XMLTYPE)/PG XPATH/T-SQL .value()), but MySQL<br>UpdateXML has no cross-engine equivalent (PG lacks it; T-SQL .modify() XML-DML and Oracle UPDATEXML<br>differ) so it degrades to NULL + annotation (docs/03-unsupported.md). fails on oracle, postgresql,<br>tsql | [03-unsupported.md](../03-unsupported.md) |
| `mysql-drop4-ZEROFILL` | mysql | — | ZEROFILL is a display-only zero pad sqlglot drops at parse; the stored value is identical, so the<br>loss is annotated + warned from the source text (docs/03-unsupported.md §3.22). fails on oracle,<br>postgresql, tsql | [03-unsupported.md §3.22](../03-unsupported.md) |
| `ora-arr-collect` | oracle | — | Oracle's SYS.ODCINUMBERLIST/ODCIVARCHAR2LIST (built-in collection types used as table/array<br>constructors) have no cross-engine equivalent; the gate now recognizes them and degrades + annotates<br>instead of shipping an undefined function (docs/03-unsupported.md). fails on mysql, postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora-collect` | oracle | — | Oracle's COLLECT aggregate builds a nested-table collection (paired with a SYS.ODCI* type) — no<br>cross-engine equivalent (PG array_agg yields an array, not an Oracle collection); the gate now<br>recognizes it and degrades + annotates (docs/03-unsupported.md). fails on postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora-day-of-week` | oracle | — | TO_CHAR(d,'D') day-of-week number is NLS_TERRITORY-dependent in Oracle, so no portable equivalent<br>exists; gated + annotated (docs/03-unsupported.md). fails on mysql | [03-unsupported.md](../03-unsupported.md) |
| `ora-edit-distance` | oracle | — | Oracle's UTL_MATCH.EDIT_DISTANCE (Levenshtein) has no core cross-engine equivalent (PG's is a<br>fuzzystrmatch extension); the gate now recognizes it and degrades + annotates instead of shipping an<br>undefined function (docs/03-unsupported.md). fails on mysql, postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora-empty-is-null` | oracle | — | fails on mysql, postgresql, tsql. Oracle stores '' as NULL so '' IS NULL is true (1) only on Oracle;<br>other engines see a real empty string (0). No faithful workaround (docs/03-unsupported.md). | [03-unsupported.md](../03-unsupported.md) |
| `ora-empty-null` | oracle | — | fails on mysql, postgresql, tsql. Oracle NVL('','x')='x' because '' is NULL there;<br>COALESCE('','x')='' elsewhere. Oracle can't represent '' apart from NULL (docs/03-unsupported.md). | [03-unsupported.md](../03-unsupported.md) |
| `ora-fmt-dayname` | oracle | — | fails on mysql. locale month/day names and Oracle Q quarter token have no reproducible cross-engine<br>format token (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ora-fmt-quarter` | oracle | — | fails on mysql. locale month/day names and Oracle Q quarter token have no reproducible cross-engine<br>format token (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ora-fmt-week` | oracle | — | TO_CHAR(d,'WW') week-of-year has no portable format token (engines disagree on week 1, and MySQL's<br>'%W' means the weekday NAME); it now degrades + annotates instead of emitting a silently wrong<br>'Monday' (docs/03-unsupported.md). fails on mysql | [03-unsupported.md](../03-unsupported.md) |
| `ora-fmt3` | oracle | — | fails on tsql. Oracle/PG numeric TO_CHAR mask (grouping pad space / currency L / sign MI / hex XX)<br>has no faithful MySQL/T-SQL FORMAT equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ora-format-currency` | oracle | — | TO_CHAR with a currency mask ($999,999.00) maps to PostgreSQL's TO_CHAR (works, live-verified<br>$1,234.50); MySQL's FORMAT() has no currency-symbol mask, so its output degrades to a carrier +<br>warning (docs/03-unsupported.md). fails on mysql | [03-unsupported.md](../03-unsupported.md) |
| `ora-forupdate-wait` | oracle | — | Oracle FOR UPDATE WAIT <n> (bounded lock wait) has no PostgreSQL/MySQL equivalent (they offer only<br>FOR UPDATE / NOWAIT); the WAIT <n> is dropped and the lost timeout annotated<br>(docs/03-unsupported.md). fails on mysql, postgresql | [03-unsupported.md](../03-unsupported.md) |
| `ora-functional-index` | oracle | — | an expression index maps to MySQL 8.0.13+/PostgreSQL double-paren form ((a*2)) [now valid there];<br>T-SQL has no expression index (needs a computed column), so it degrades with a carrier<br>(docs/03-unsupported.md). fails on tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora-identity-opts` | oracle | — | MySQL AUTO_INCREMENT has no per-column START/INCREMENT/MAXVALUE/CYCLE; emits AUTO_INCREMENT (keyed)<br>+ a documented carrier + warning (docs/03-unsupported.md). fails on mysql | [03-unsupported.md](../03-unsupported.md) |
| `ora-initcap` | oracle | — | INITCAP (title-case each word) has no MySQL/T-SQL builtin and cannot be emulated for arbitrary<br>multi-word text; PostgreSQL has INITCAP natively so it transpiles cleanly there. Gated + annotated<br>on the other two (docs/03-unsupported.md). fails on mysql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora-instr-empty` | oracle | — | fails on mysql, postgresql, tsql. Oracle INSTR(s,'') is NULL ('' is NULL); other engines return 0.<br>No faithful workaround (docs/03-unsupported.md). | [03-unsupported.md](../03-unsupported.md) |
| `ora-interval-tochar` | oracle | — | each engine renders an INTERVAL as text in its own default format (Oracle '+02 03:04:05.000000' vs<br>PG '2 days 03:04:05'); TO_CHAR of an interval has no portable mask — annotated + warned<br>(docs/03-unsupported.md §3.22). fails on postgresql | [03-unsupported.md §3.22](../03-unsupported.md) |
| `ora-json-xml-agg` | oracle | — | combines JSON_ARRAYAGG + XMLAGG; PostgreSQL has both natively (works, live-verified [1,2] /<br><i>1</i><i>2</i>), but MySQL/T-SQL have no XML aggregate, so their output degrades to a carrier +<br>warning (docs/03-unsupported.md). fails on mysql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora-listagg-over` | oracle | — | Oracle LISTAGG(...) WITHIN GROUP (...) OVER (...) is windowed string aggregation; T-SQL STRING_AGG<br>(4113) and MySQL GROUP_CONCAT (1235) are never window functions and PG rejects an ORDER-BY'd<br>aggregate as a window fn — no equivalent, degrades to NULL + annotation (docs/03-unsupported.md).<br>fails on mysql, postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora-month-name` | oracle | — | fails on mysql. TO_CHAR with a locale month/day NAME (Month/Day) is NLS-dependent, no reproducible<br>cross-engine equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ora-multiset-table` | oracle | — | fails on postgresql, tsql. Oracle collection unnesting TABLE(CAST(MULTISET(...) AS <collection>))<br>has no PG/T-SQL equivalent; degrades to a carrier (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ora-name-locale` | oracle | — | fails on mysql. TO_CHAR with locale month/day NAMES (Day/Month) is NLS/collation-dependent, no<br>cross-engine equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ora-numfmt-lead` | oracle | — | fails on mysql. Oracle/PG numeric TO_CHAR mask (grouping pad space / currency L / sign MI / hex XX)<br>has no faithful MySQL/T-SQL FORMAT equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ora-numfmt-sign` | oracle | — | fails on mysql. Oracle/PG numeric TO_CHAR mask (grouping pad space / currency L / sign MI / hex XX)<br>has no faithful MySQL/T-SQL FORMAT equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ora-numfmt-spec` | oracle | — | fails on tsql. Oracle/PG numeric TO_CHAR mask (grouping pad space / currency L / sign MI / hex XX)<br>has no faithful MySQL/T-SQL FORMAT equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ora-numfmt-thousands` | oracle | — | fails on mysql. Oracle/PG numeric TO_CHAR mask (grouping pad space / currency L / sign MI / hex XX)<br>has no faithful MySQL/T-SQL FORMAT equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ora-rand` | oracle | — | fails on mysql, postgresql, tsql. DBMS_RANDOM.VALUE/STRING is non-deterministic (values cannot match<br>cross-engine) and DBMS_RANDOM.STRING has no equivalent random-string builtin elsewhere<br>(docs/03-unsupported.md §2). Warned carrier on all three. | [03-unsupported.md §2](../03-unsupported.md) |
| `ora-regexp-group` | oracle | — | MySQL REGEXP_SUBSTR has no capture-group argument; emit the portable (str,pat,pos,occ) subset + a<br>documented carrier (docs/03-unsupported.md). fails on mysql | [03-unsupported.md](../03-unsupported.md) |
| `ora-seq-use` | oracle | — | fails on tsql. NEXTVAL maps to NEXT VALUE FOR; T-SQL has no CURRVAL (current value without<br>advancing) — degrades to a carrier (docs/03-unsupported.md §3.17). | [03-unsupported.md §3.17](../03-unsupported.md) |
| `ora-table-collection` | oracle | — | Oracle TABLE(SYS.ODCINUMBERLIST(...)) unnests a built-in collection into rows; no cross-engine<br>equivalent (the ODCI collection type + TABLE() have no PG/T-SQL form), so it degrades to a carrier +<br>warning (docs/03-unsupported.md). fails on postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora-table-fn2` | oracle | — | TABLE(SYS.ODCINUMBERLIST(...)) collection unnesting with COLUMN_VALUE has no PG/T-SQL equivalent;<br>degrades to a carrier + warning (docs/03-unsupported.md). fails on postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora-table-varchar-list` | oracle | — | TABLE(SYS.ODCIVARCHAR2LIST(...)) collection unnesting has no PG/T-SQL equivalent; degrades to a<br>carrier + warning (docs/03-unsupported.md). fails on postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora-to-char-day` | oracle | — | fails on mysql. TO_CHAR(d,'DAY') is a locale day NAME, NLS-dependent, no reproducible cross-engine<br>equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ora-tochar-long` | oracle | — | fails on postgresql, tsql. date format mask uses a bare-letter literal / locale name / exotic token<br>that cannot round-trip to a quoted cross-engine mask (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ora-tochar-neg` | oracle | — | fails on mysql. Oracle/PG numeric TO_CHAR mask (grouping pad space / currency L / sign MI / hex XX)<br>has no faithful MySQL/T-SQL FORMAT equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ora-trailing-eq` | oracle | — | fails on tsql. APPROVED LIMIT (2026-07-18): string-comparison collation (case/accent/trailing-space)<br>is a per-column/default-collation property, not statement-compensable (docs/03-unsupported.md §2).<br>FUNC-DIFF: source=(('0',),) target=(('1',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `ora-trailing-space-cmp` | oracle | — | fails on tsql. APPROVED LIMIT (2026-07-18): string-comparison collation (case/accent/trailing-space)<br>is a per-column/default-collation property, not statement-compensable (docs/03-unsupported.md §2).<br>FUNC-DIFF: source=(('0', '0'),) target=(('1', '1'),) | [03-unsupported.md §2](../03-unsupported.md) |
| `ora-translate3` | oracle | — | Oracle 2-arg REPLACE(s, search) rewritten to NULLIF(REPLACE(s, search, ''), '') on all targets<br>(reproduces Oracle's empty->NULL; NULL/'abc45' verified on PG/T-SQL); TRANSLATE is native on<br>PG/T-SQL but MySQL has none (nested REPLACE is order-dependent) so it degrades there<br>(docs/03-unsupported.md). fails on mysql | [03-unsupported.md](../03-unsupported.md) |
| `ora-tz-funcs` | oracle | — | SYSTIMESTAMP/LOCALTIMESTAMP map to each target's now-functions and SESSIONTIMEZONE to its session-<br>zone expression (current_setting('TimeZone') / @@session.time_zone / DATENAME(TZOFFSET,<br>SYSDATETIMEOFFSET())), annotated + warned: the values are session/server-dependent and cannot match<br>across engines (docs/03-unsupported.md; ts-spid-version precedent). fails on mysql, postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora-utl-raw` | oracle | — | Oracle's UTL_RAW.CAST_TO_RAW (RAW/byte packing) has no cross-engine equivalent; the gate now<br>recognizes the UTL_RAW package functions and degrades + annotates instead of shipping an undefined<br>function (docs/03-unsupported.md). fails on mysql, postgresql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora-xmlagg` | oracle | — | XMLAGG (XML fragment aggregation) has no MySQL or T-SQL equivalent; PostgreSQL has xmlagg/xmlelement<br>natively (works, live-verified <e>x</e>). The MySQL/T-SQL output degrades to a carrier + warning<br>(docs/03-unsupported.md). fails on mysql, tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora-xmlelement` | oracle | — | fails on mysql, tsql. XMLELEMENT is an SQL/XML built-in on Oracle & PostgreSQL (transpiled<br>faithfully, element-name case preserved); MySQL has no XML type and T-SQL has no XMLELEMENT (only<br>FOR XML) — no cross-engine mapping (docs/03-unsupported.md §5, §2). Warned carrier on mysql/tsql. | [03-unsupported.md §5](../03-unsupported.md) |
| `ora-xmltable` | oracle | — | XMLTABLE is a table-valued XML shredder; PostgreSQL's XMLTABLE has a different column-spec shape and<br>T-SQL has no equivalent (it uses .nodes()/.value()), so the statement degrades to a carrier +<br>warning rather than shipping the undefined function (docs/03-unsupported.md). fails on postgresql,<br>tsql | [03-unsupported.md](../03-unsupported.md) |
| `ora23-json-object-star` | oracle | — | JSON_OBJECT(*) star-expansion needs the table's schema (no target can expand it statically); the<br>statement degrades to a documented carrier + warning off Oracle (docs/03-unsupported.md). fails on<br>postgresql | [03-unsupported.md](../03-unsupported.md) |
| `pg-accent-eq` | postgresql | — | fails on mysql. APPROVED LIMIT (2026-07-18): string-comparison collation (case/accent/trailing-<br>space) is a per-column/default-collation property, not statement-compensable (docs/03-unsupported.md<br>§2). FUNC-DIFF: source=(('0',),) target=(('1',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `pg-array-jsonb` | postgresql | — | PostgreSQL array column types (TEXT[], INT[][]) have no cross-engine model (docs/03-unsupported.md<br>§7); the output gate now degrades the statement to a documented carrier + warning off PG (was<br>shipped invalid, ORA-03099). fails on oracle | [03-unsupported.md §7](../03-unsupported.md) |
| `pg-at-time-zone` | postgresql | — | AT TIME ZONE is not portable (Oracle/MySQL have no such operator; the session-tz-dependent display<br>differs on PG/T-SQL), so it degrades to NULL + annotation off PG (docs/03-unsupported.md). fails on<br>mysql, oracle, tsql | [03-unsupported.md](../03-unsupported.md) |
| `pg-cast-datetime2` | postgresql | — | the ::time and ::interval casts have no Oracle type; kept as text with documented carriers<br>(docs/03-unsupported.md). fails on oracle | [03-unsupported.md](../03-unsupported.md) |
| `pg-cast-interval` | postgresql | — | Oracle has no bare INTERVAL type (it needs a DAY TO SECOND/YEAR TO MONTH qualifier); '1<br>day'::interval keeps the value as text with a documented carrier (docs/03-unsupported.md). fails on<br>oracle | [03-unsupported.md](../03-unsupported.md) |
| `pg-cast-money` | postgresql | — | money maps to NUMBER(19,4) (valid, same numeric value) but PostgreSQL renders money with a currency<br>symbol ('$12.99') and Oracle renders it plain — formatted-text divergence, annotated + warned per<br>docs/03-unsupported.md §3.20. fails on oracle | [03-unsupported.md §3.20](../03-unsupported.md) |
| `pg-cast-point` | postgresql | — | PostgreSQL's geometric point type has no cross-engine equivalent (MySQL's spatial POINT is a<br>different WKB type); the cast keeps the source's text value '(1,2)' + annotation<br>(docs/03-unsupported.md). fails on oracle, tsql | [03-unsupported.md](../03-unsupported.md) |
| `pg-datetrunc-units` | postgresql | — | fails on mysql, oracle, tsql. date_trunc('decade', …) has no cross-engine equivalent and now() is<br>non-deterministic (docs/03-unsupported.md §2). | [03-unsupported.md §2](../03-unsupported.md) |
| `pg-empty-is-null` | postgresql | — | fails on oracle. APPROVED LIMIT (2026-07-19): Oracle stores '' as NULL so `'' IS NULL` is true<br>(false on PostgreSQL) — no faithful workaround (docs/03-unsupported.md). Warns + annotates UNIQUE:<br>on Oracle. FUNC-DIFF: source=(('0',),) target=(('1',),) | [03-unsupported.md](../03-unsupported.md) |
| `pg-epoch` | postgresql | — | EXTRACT(EPOCH FROM timestamp) is translated to a literal date-diff (1577836800 verified), but<br>EXTRACT(EPOCH FROM interval) has no portable equivalent (T-SQL/MySQL have no interval value type) so<br>it degrades to NULL + annotation (docs/03-unsupported.md). fails on mysql, oracle, tsql | [03-unsupported.md](../03-unsupported.md) |
| `pg-exception-handler` | postgresql | — | WHEN OTHERS handler compiles on Oracle/MySQL; T-SQL scalar functions forbid TRY/CATCH (error 443) so<br>it degrades to a carrier (docs/03-unsupported.md). fails on tsql | [03-unsupported.md](../03-unsupported.md) |
| `pg-exception-when` | postgresql | — | unique_violation/OTHERS handler compiles on Oracle/MySQL (with the referenced table present); T-SQL<br>scalar functions forbid TRY/CATCH (error 443) so it degrades to a carrier (docs/03-unsupported.md).<br>fails on tsql | [03-unsupported.md](../03-unsupported.md) |
| `pg-fcollate` | postgresql | — | fails on mysql, tsql. APPROVED LIMIT (2026-07-18): string-comparison collation<br>(case/accent/trailing-space) is a per-column/default-collation property, not statement-compensable<br>(docs/03-unsupported.md §2). FUNC-DIFF: source=(('c', 'B', '0'),) target=(('c', 'a', '1'),) | [03-unsupported.md §2](../03-unsupported.md) |
| `pg-fmt-spec` | postgresql | — | fails on oracle. date format mask uses a bare-letter literal / locale name / exotic token that<br>cannot round-trip to a quoted cross-engine mask (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `pg-fmt3` | postgresql | — | fails on oracle. Oracle/PG numeric TO_CHAR mask (grouping pad space / currency L / sign MI / hex XX)<br>has no faithful MySQL/T-SQL FORMAT equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `pg-format-currency` | postgresql | — | fails on mysql, tsql. currency-symbol number formatting has no cross-engine equivalent<br>(docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `pg-frac-seconds` | postgresql | — | EXTRACT(MICROSECONDS FROM t) = SECOND*1e6 + MICROSECOND on MySQL/T-SQL (30123456 verified; MySQL<br>DATETIME/TIME casts of a fractional literal now use (6) to keep the sub-second part); Oracle has no<br>TIME type so the TIME-based extract degrades to a carrier (docs/03-unsupported.md). fails on oracle | [03-unsupported.md](../03-unsupported.md) |
| `pg-gen-series-date` | postgresql | — | date-range generate_series(date, date, INTERVAL 'n' DAY) rewritten for Oracle (DATE + (LEVEL-1)*n)<br>and T-SQL (DATEADD over a numbers source); the 5 dates match (PG returns timestamptz, Oracle/T-SQL<br>date/timestamp — precision only). MySQL has no inline table function so it degrades<br>(docs/03-unsupported.md). fails on mysql | [03-unsupported.md](../03-unsupported.md) |
| `pg-generate-series` | postgresql | — | a SELECT-list generate_series (SRF) is moved to FROM and rewritten for Oracle (CONNECT BY) and T-SQL<br>(numbers source) — 1..5 verified; MySQL has no inline table function (a recursive CTE can't be<br>inlined in FROM) so it degrades (docs/03-unsupported.md). fails on mysql | [03-unsupported.md](../03-unsupported.md) |
| `pg-grouping-fn` | postgresql | — | GROUPING(x) over GROUP BY CUBE works on Oracle/T-SQL ((1,0)/(NULL,1) verified); MySQL has no CUBE so<br>it degrades to the base grouping (subtotal rows omitted) where GROUPING is always 0 — the (1,0) row<br>matches (docs/03-unsupported.md). fails on mysql | [03-unsupported.md](../03-unsupported.md) |
| `pg-hash-fns` | postgresql | — | lpad + md5 translate faithfully (Oracle LOWER(STANDARD_HASH(x,'MD5')), T-SQL<br>LOWER(CONVERT(...,HASHBYTES('MD5',x),2)); '  x'/'9dd4e4...' verified), but PG sha256(bytea) returns<br>a bytea digest where the other engines return a hex string (same digest, different representation)<br>so it degrades to NULL + annotation (docs/03-unsupported.md). fails on mysql, oracle, tsql | [03-unsupported.md](../03-unsupported.md) |
| `pg-json-aggs` | postgresql | — | fails on tsql. json_agg/json_object_agg map faithfully to MySQL/Oracle JSON_ARRAYAGG/JSON_OBJECTAGG<br>(Oracle key cast to VARCHAR2; value-verified); T-SQL has no JSON aggregate (docs/03-unsupported.md<br>§3.9). Warned carrier on tsql. | [03-unsupported.md §3.9](../03-unsupported.md) |
| `pg-like-cs` | postgresql | — | fails on mysql, tsql. APPROVED LIMIT (2026-07-18): string-comparison collation<br>(case/accent/trailing-space) is a per-column/default-collation property, not statement-compensable<br>(docs/03-unsupported.md §2). FUNC-DIFF: source=(('0',),) target=(('1',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `pg-loop-notice` | postgresql | — | LOOP + RAISE NOTICE compiles on Oracle/MySQL; T-SQL scalar functions forbid PRINT/side-effects<br>(error 443) so it degrades to a carrier (docs/03-unsupported.md). fails on tsql | [03-unsupported.md](../03-unsupported.md) |
| `pg-name-locale` | postgresql | — | fails on mysql, tsql. to_char with locale month/day NAMES (Day/Month/FMDay) is locale-dependent, no<br>cross-engine equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `pg-named-exception` | postgresql | — | the named exception maps per engine (PG division_by_zero -> Oracle ZERO_DIVIDE), valid + f()=-1 on<br>Oracle/MySQL; T-SQL scalar functions forbid TRY/CATCH (error 443) so the exception handler has no<br>function-level equivalent and degrades to a carrier (docs/03-unsupported.md). fails on tsql | [03-unsupported.md](../03-unsupported.md) |
| `pg-nan-cmp` | postgresql | — | PostgreSQL numeric NaN (NaN > 1 = true) has no MySQL equivalent (CAST('NaN' AS DECIMAL) collapses to<br>0); emit the cast + a documented carrier (docs/03-unsupported.md). fails on mysql | [03-unsupported.md](../03-unsupported.md) |
| `pg-not-null-is-null` | postgresql | — | fails on tsql. (NOT NULL) IS NULL is 1 (3VL: NOT NULL is NULL, which IS null); the IR dropped the<br>source parens so NOT re-associated. Parens restored -> live 1 on PG/MySQL/Oracle; T-SQL has no<br>boolean value type (NOT of a non-predicate is error 4145) so it degrades to a carrier<br>(docs/03-unsupported.md §3.18). | [03-unsupported.md §3.18](../03-unsupported.md) |
| `pg-numfmt-lead` | postgresql | — | fails on mysql. Oracle/PG numeric TO_CHAR mask (grouping pad space / currency L / sign MI / hex XX)<br>has no faithful MySQL/T-SQL FORMAT equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `pg-numfmt-spec` | postgresql | — | fails on oracle. Oracle/PG numeric TO_CHAR mask (grouping pad space / currency L / sign MI / hex XX)<br>has no faithful MySQL/T-SQL FORMAT equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `pg-numfmt-thousands` | postgresql | — | fails on mysql, tsql. Oracle/PG numeric TO_CHAR mask (grouping pad space / currency L / sign MI /<br>hex XX) has no faithful MySQL/T-SQL FORMAT equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `pg-str-lt` | postgresql | — | fails on mysql, tsql. APPROVED LIMIT (2026-07-18): string-comparison collation<br>(case/accent/trailing-space) is a per-column/default-collation property, not statement-compensable<br>(docs/03-unsupported.md §2). FUNC-DIFF: source=(('0',),) target=(('1',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `pg-substring-escape` | postgresql | — | SUBSTRING(x FROM SIMILAR-TO pattern FOR escape) is the SQL-standard regex form; its %/_ metachars<br>and #"…"# capture markers differ from POSIX, so no faithful cross-engine rewrite exists — degrades<br>to NULL + annotation (docs/03-unsupported.md). fails on oracle, tsql | [03-unsupported.md](../03-unsupported.md) |
| `pg-substring-regex` | postgresql | — | SUBSTRING(x FROM POSIX pattern) → Oracle/MySQL REGEXP_SUBSTR ('1' verified live); T-SQL has no POSIX<br>regex engine (2012+ target) so it degrades to NULL + annotation (docs/03-unsupported.md). fails on<br>tsql | [03-unsupported.md](../03-unsupported.md) |
| `pg-tablesample` | postgresql | — | MySQL has no TABLESAMPLE (row sampling is also non-deterministic); degraded to a documented carrier<br>+ warning (docs/03-unsupported.md). PG->T-SQL/Oracle sample natively. fails on mysql | [03-unsupported.md](../03-unsupported.md) |
| `pg-tochar-fmts` | postgresql | — | fails on oracle. date format mask uses a bare-letter literal / locale name / exotic token that<br>cannot round-trip to a quoted cross-engine mask (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `pg-tochar-neg` | postgresql | — | fails on mysql, tsql. Oracle/PG numeric TO_CHAR mask (grouping pad space / currency L / sign MI /<br>hex XX) has no faithful MySQL/T-SQL FORMAT equivalent (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `pg-tohex2` | postgresql | — | PostgreSQL to_char(n,'XX') emits the literal template 'XX' (it has no hex number format), whereas<br>Oracle's TO_CHAR reads X as hex (' FF'); the mask is not portable so the statement is gated +<br>annotated (docs/03-unsupported.md). fails on oracle, tsql | [03-unsupported.md](../03-unsupported.md) |
| `pg-totimestamp-long` | postgresql | — | fails on mysql, oracle, tsql. parsing a locale month NAME ('Month DD YYYY') is NLS-dependent, no<br>reproducible cross-engine parse (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `pg-trailing-eq` | postgresql | — | fails on oracle, tsql. APPROVED LIMIT (2026-07-18): string-comparison collation<br>(case/accent/trailing-space) is a per-column/default-collation property, not statement-compensable<br>(docs/03-unsupported.md §2). FUNC-DIFF: source=(('0',),) target=(('1',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `pg-trailing-space-cmp` | postgresql | — | fails on mysql, oracle, tsql. APPROVED LIMIT (2026-07-18): string-comparison collation<br>(case/accent/trailing-space) is a per-column/default-collation property, not statement-compensable<br>(docs/03-unsupported.md §2). FUNC-DIFF: source=(('0', '1', '0'),) target=(('1', '1', '1'),) | [03-unsupported.md §2](../03-unsupported.md) |
| `pg-tz-convert` | postgresql | — | AT TIME ZONE is not portable (Oracle/MySQL have no such operator; the session-tz-dependent display<br>differs on PG/T-SQL) and degrades to NULL + annotation + warning off PG — the same approved limit as<br>ts-at-time-zone (docs/03-unsupported.md). fails on mysql, oracle, tsql | [03-unsupported.md](../03-unsupported.md) |
| `pg-unicode-escape` | postgresql | — | U&'…' Unicode-escape literals are mis-parsed by the parser (U & '…'), so the statement degrades to a<br>documented carrier + warning off PG instead of shipping a silently wrong expression<br>(docs/03-unsupported.md §3.22). fails on mysql, oracle, tsql | [03-unsupported.md §3.22](../03-unsupported.md) |
| `pg-unique-nulls-notdistinct` | postgresql | — | PostgreSQL UNIQUE … NULLS NOT DISTINCT (NULLs compare equal, so only one NULL row is allowed) has no<br>equivalent; the modifier is stripped to a plain UNIQUE (NULLs distinct) and the divergence is<br>annotated (docs/03-unsupported.md). fails on mysql, oracle | [03-unsupported.md](../03-unsupported.md) |
| `pg-window-groups-frame` | postgresql | `invalid` | a GROUPS window-frame mode (OVER (ORDER BY x GROUPS BETWEEN 1 PRECEDING AND CURRENT ROW)) has no<br>ROWS/RANGE equivalent and fails on sqlserver, mysql — degraded there to a warned NULL carrier<br>(maintainer-approved 2026-07-30); Oracle keeps the native GROUPS frame. See docs/03-unsupported.md<br>3.25 (GROUPS window frame). | [03-unsupported.md §3.25](../03-unsupported.md) |
| `pg-xmlelement` | postgresql | — | fails on mysql, tsql. XMLELEMENT maps faithfully between PostgreSQL & Oracle, but MySQL/T-SQL have<br>no XMLELEMENT (docs/03-unsupported.md §5, §2). Warned carrier on mysql/tsql. | [03-unsupported.md §5](../03-unsupported.md) |
| `pg-xmlelement2` | postgresql | — | fails on mysql, tsql. Lowercase spelling of pg-xmlelement; XMLELEMENT has no MySQL/T-SQL equivalent<br>(docs/03-unsupported.md §5, §2). Warned carrier on mysql/tsql. | [03-unsupported.md §5](../03-unsupported.md) |
| `po-agg-bit` | postgresql | — | fails on oracle, tsql. BIT_AND/BIT_OR/BIT_XOR aggregates map faithfully PostgreSQL<->MySQL (value-<br>verified); Oracle/T-SQL have no bit aggregate (docs/03-unsupported.md §3.10). Warned carrier on<br>oracle/tsql. | [03-unsupported.md §3.10](../03-unsupported.md) |
| `ts-at-time-zone` | sqlserver | — | AT TIME ZONE is not portable (Oracle/MySQL have no such operator; the session-tz-dependent display<br>differs on PG/T-SQL), so it degrades to NULL + annotation off T-SQL (docs/03-unsupported.md). fails<br>on oracle, postgresql | [03-unsupported.md](../03-unsupported.md) |
| `ts-bit-fns` | sqlserver | — | GET_BIT/SET_BIT have no cross-engine builtin; gated + annotated (docs/03-unsupported.md). fails on<br>mysql, oracle, postgresql | [03-unsupported.md](../03-unsupported.md) |
| `ts-cursor-attr` | sqlserver | — | fails on mysql, oracle, postgresql. @@CURSOR_ROWS has no equivalent (neutral-0 carrier;<br>docs/03-unsupported.md §7) and FETCH without INTO is preserved as a comment. The Oracle<br>PLS-00103/ORA-00906 CAST-context inversion is handled: the PRINT argument is a marked expression<br>position, so its char CAST stays lengthless. All three targets compile (live 2026-07-24). | [03-unsupported.md §7](../03-unsupported.md) |
| `ts-fmt-spec` | sqlserver | — | fails on oracle. date format mask uses a bare-letter literal / locale name / exotic token that<br>cannot round-trip to a quoted cross-engine mask (docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ts-for-xml` | sqlserver | — | T-SQL FOR XML/JSON serializes a row set into a single XML/JSON scalar; no other engine has an<br>equivalent (dropping it ships the multi-column rows raw → ORA-00913), so the scalar subquery<br>degrades to NULL + annotation (docs/03-unsupported.md). fails on mysql, oracle, postgresql | [03-unsupported.md](../03-unsupported.md) |
| `ts-format-iso` | sqlserver | — | fails on oracle. T-SQL FORMAT numeric/date .NET mask with no reproducible cross-engine equivalent<br>(docs/03-unsupported.md §3.1). | [03-unsupported.md §3.1](../03-unsupported.md) |
| `ts-geography` | sqlserver | — | T-SQL geography/geometry CLR type methods (the ``type::method()`` ScopeResolution) have no cross-<br>engine equivalent; sqlglot silently flattened them, so the construct now degrades to NULL +<br>annotation instead of shipping mangled invalid SQL (docs/03-unsupported.md). fails on mysql, oracle,<br>postgresql | [03-unsupported.md](../03-unsupported.md) |
| `ts-hash-all` | sqlserver | — | CHECKSUM is a proprietary T-SQL row hash with no cross-engine equivalent; gated + annotated<br>(docs/03-unsupported.md). fails on mysql, postgresql | [03-unsupported.md](../03-unsupported.md) |
| `ts-identity-funcs` | sqlserver | — | T-SQL identity-scope reads (SCOPE_IDENTITY/@@IDENTITY/IDENT_CURRENT) have no cross-engine equivalent<br>(each engine exposes the last identity differently: Oracle sequence.CURRVAL, PG lastval(), MySQL<br>LAST_INSERT_ID()); the statement degrades to a carrier + warning (docs/03-unsupported.md). fails on<br>mysql, oracle, postgresql | [03-unsupported.md](../03-unsupported.md) |
| `ts-insert-output` | sqlserver | — | the T-SQL OUTPUT clause returns a result set; PostgreSQL maps it to RETURNING, but Oracle's<br>RETURNING needs INTO variables (PL/SQL only, ORA-63809), so the INSERT runs and the OUTPUT is<br>documented in a carrier (docs/03-unsupported.md). fails on oracle | [03-unsupported.md](../03-unsupported.md) |
| `ts-now-fns` | sqlserver | — | fails on mysql, oracle, postgresql. current-time functions are non-deterministic (no cross-engine<br>value parity) and SYSDATETIMEOFFSET has no equivalent (docs/03-unsupported.md §2). | [03-unsupported.md §2](../03-unsupported.md) |
| `ts-openjson` | sqlserver | — | OPENJSON is a T-SQL table-valued JSON shredder with no simple cross-engine form (Oracle JSON_TABLE,<br>PostgreSQL json_array_elements have different shapes); the statement degrades to a carrier + warning<br>rather than shipping the undefined function (docs/03-unsupported.md). fails on oracle, postgresql | [03-unsupported.md](../03-unsupported.md) |
| `ts-scroll-cursor` | sqlserver | — | a scroll cursor FETCH (PRIOR/FIRST/LAST/ABSOLUTE/RELATIVE) has no cross-engine equivalent<br>(Oracle/PG/MySQL cursors are forward-only, only FETCH NEXT); the scroll fetch degrades to a carrier<br>comment and the surrounding OPEN/CLOSE compile (docs/03-unsupported.md). fails on mysql, oracle,<br>postgresql | [03-unsupported.md](../03-unsupported.md) |
| `ts-spatial` | sqlserver | — | T-SQL geometry/geography CLR type methods (``type::Point(…).STDistance(…)`` ScopeResolution) have no<br>cross-engine equivalent; degraded to NULL + annotation instead of the mangled invalid flatten<br>(docs/03-unsupported.md). fails on oracle, postgresql | [03-unsupported.md](../03-unsupported.md) |
| `ts-spid-version` | sqlserver | — | @@SPID/@@VERSION map to each engine's own session-id/version function, but the values are server-<br>and connection-specific and can never equal T-SQL's; mapped + annotated (docs/03-unsupported.md).<br>fails on mysql, oracle, postgresql | [03-unsupported.md](../03-unsupported.md) |
| `ts-st-distance` | sqlserver | — | T-SQL geometry ``::Point(…).STDistance(…)`` (a CLR ScopeResolution method) has no cross-engine<br>equivalent; degraded to NULL + annotation (docs/03-unsupported.md). fails on oracle, postgresql | [03-unsupported.md](../03-unsupported.md) |
| `ts-tablesample` | sqlserver | — | MySQL has no TABLESAMPLE (row sampling is also non-deterministic); degraded to a documented carrier<br>+ warning (docs/03-unsupported.md). fails on mysql | [03-unsupported.md](../03-unsupported.md) |
| `ts-trailing-eq` | sqlserver | — | fails on mysql, oracle, postgresql. APPROVED LIMIT (2026-07-18): string-comparison collation<br>(case/accent/trailing-space) is a per-column/default-collation property, not statement-compensable<br>(docs/03-unsupported.md §2). FUNC-DIFF: source=(('1',),) target=(('0',),) | [03-unsupported.md §2](../03-unsupported.md) |
| `ts-trailing-space-cmp` | sqlserver | — | fails on mysql, oracle, postgresql. APPROVED LIMIT (2026-07-18): string-comparison collation<br>(case/accent/trailing-space) is a per-column/default-collation property, not statement-compensable<br>(docs/03-unsupported.md §2). FUNC-DIFF: source=(('eq', 'eq'),) target=(('ne', 'ne'),) | [03-unsupported.md §2](../03-unsupported.md) |
| `ts-tzoffset` | sqlserver | — | fails on mysql, oracle, postgresql. DATENAME(TZOFFSET, SYSDATETIMEOFFSET()) is non-deterministic and<br>tz-offset extraction is engine-specific (docs/03-unsupported.md §2). | [03-unsupported.md §2](../03-unsupported.md) |
| `ts-update-output` | sqlserver | — | same as ts-insert-output: OUTPUT has no standalone Oracle equivalent (RETURNING needs INTO<br>variables); the UPDATE runs and the OUTPUT is documented in a carrier (docs/03-unsupported.md).<br>fails on oracle | [03-unsupported.md](../03-unsupported.md) |

169 `[limit]` cases across 4 source engine(s).
